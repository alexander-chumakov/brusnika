---
phase: 03.1-day-night-theme
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/components/AudioPlayer.astro
  - src/components/LessonsModal.astro
  - src/components/VideoSection.astro
  - src/components/GallerySection.astro
autonomous: true
requirements: [D-06, UISPEC-STEP-1]
must_haves:
  truths:
    - "In day mode the now-playing player bar is recolored (not a dark island)"
    - "In day mode the lessons modal and video modal backdrops, dialogs, and inputs are recolored"
    - "The PhotoSwipe lightbox and gallery checkerboard recolor with the theme"
    - "Night theme for player + modals + gallery is pixel-identical to before"
  artifacts:
    - path: "src/components/AudioPlayer.astro"
      provides: "Tokenized player bar surface, border, button hover, focus ring"
      contains: "var(--color-surface-player)"
    - path: "src/components/LessonsModal.astro"
      provides: "Tokenized backdrop, dialog bg, input fields, dividers"
      contains: "var(--color-overlay-scrim)"
    - path: "src/components/GallerySection.astro"
      provides: "Tokenized checkerboard + PhotoSwipe --pswp-* vars + caption pill"
      contains: "var(--color-pswp-bg)"
  key_links:
    - from: "GallerySection .pswp --pswp-bg/--pswp-icon-color"
      to: "global.css theme tokens via html[data-theme] cascade"
      via: "var(--color-pswp-bg) etc."
      pattern: "--pswp-bg:\\s*var\\(--color-pswp-bg\\)"
    - from: "player + modal surfaces"
      to: "global.css [data-theme=day] tokens (defined in 03.1-01)"
      via: "var(--color-surface-*) / var(--color-overlay-*)"
      pattern: "var\\(--color-(surface|overlay)"
---

<objective>
Tokenize the D-06-critical "dark-island risk" surfaces: the now-playing player bar (AudioPlayer), the lessons modal (LessonsModal), the video card + Shorts modal (VideoSection), and the gallery checkerboard + PhotoSwipe lightbox + caption pill (GallerySection). These are the surfaces most likely to stay dark in day mode; tokenizing them satisfies "full theming — no dark islands." No tokens are defined here — only consumed (defined in Plan 01).

Purpose: Delivers D-06 for the interactive/overlay surfaces. These files have zero overlap with Plan 02's section files, so the two run in parallel in Wave 2.

Output: Player bar, both modals, and the gallery/lightbox fully token-driven; night unchanged; day mode recolors them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md
@.planning/phases/03.1-day-night-theme/03.1-PATTERNS.md
@.planning/phases/03.1-day-night-theme/03.1-01-PLAN.md

<interfaces>
<!-- Tokens DEFINED in 03.1-01 (src/styles/global.css). This plan only consumes them. -->
<!-- Canonical name/night/day values: 03.1-UI-SPEC.md "Complete Token Contract". Key ones here: -->
- --color-surface-player / --color-surface-player-border  (now-playing bar)
- --color-accent-tint-strong  (player button hover, modal accent tint)
- --color-accent-ring  (focus ring box-shadow)
- --color-overlay-scrim  (lessons modal backdrop, gallery caption pill)
- --color-overlay-dark   (video modal backdrop)
- --color-bg-modal       (modal dialog background)
- --color-surface-card   (modal input field background)
- --color-surface-video  (video card background)
- --color-border / --color-accent-stroke  (modal dividers / accent divider)
- --color-bg             (video modal container)
- --color-gallery-bg / --color-gallery-bg-alt  (checkerboard placeholder)
- --color-pswp-bg / --color-text / --color-text-subtle / --color-accent  (PhotoSwipe --pswp-* vars)
- --color-text-on-accent  (#000 night / #fff day — text/icons ON pink)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize AudioPlayer now-playing bar (D-06 critical surface)</name>
  <files>src/components/AudioPlayer.astro</files>
  <read_first>
    - src/components/AudioPlayer.astro (player bar `.np-bar` bg + border-top lines 460-461; button hover + focus-ring box-shadows lines 595, 608, 619, 626, 632, 641)
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (section "src/components/AudioPlayer.astro" — line-by-line literal->token map + add per-surface transition)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (Surface Inventory: now-playing bar, player border, button hover, focus ring; Transition on Toggle)
    - $HOME/.claude/projects ... (Player excerpts-minimal memory: do not add UI; this is recolor-only)
  </read_first>
  <action>
    AudioPlayer.astro: `.np-bar { background: rgba(6,6,6,0.92) }` -> `var(--color-surface-player)`; `.np-bar { border-top: 1px solid rgba(243,169,189,0.4) }` -> `var(--color-surface-player-border)`. Button hover backgrounds `rgba(243,169,189,0.22)` -> `var(--color-accent-tint-strong)`. Focus-ring box-shadows `0 0 0 3px rgba(243,169,189,0.18)` -> `var(--color-accent-ring)`; the slightly stronger `0 0 0 4px rgba(243,169,189,0.24)` -> also `var(--color-accent-ring)` (UI-SPEC defines a single ring token; accept the minor night-value harmonization, or if the 0.24 ring visibly differs keep it and note the discrepancy — prefer the single token). Add a per-surface transition `transition: background-color 0.25s ease, border-color 0.25s ease` to `.np-bar` (and to button surfaces if they have their own bg) per UI-SPEC § Transition — do NOT transition transform/opacity (would fight the player's own animations). Any text/icon sitting on the pink accent that uses `color:#000` -> `var(--color-text-on-accent)`. Do NOT add any new player UI — this is recolor only (player stays minimal excerpts player). Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "player surface token:"; grep -c "var(--color-surface-player)" src/components/AudioPlayer.astro; echo "player raw bar bg (expect 0):"; grep -Ec "rgba\(6, ?6, ?6, ?0.92\)" src/components/AudioPlayer.astro; echo "ring token:"; grep -c "var(--color-accent-ring)" src/components/AudioPlayer.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--color-surface-player)" src/components/AudioPlayer.astro` returns >= 1 and the raw `rgba(6,6,6,0.92)` bar bg returns 0
    - `var(--color-surface-player-border)` present (no raw `rgba(243,169,189,0.4)` border-top)
    - Focus-ring uses `var(--color-accent-ring)`; player button hover uses `var(--color-accent-tint-strong)`
    - `.np-bar` carries a `background-color`/`border-color` transition (not transform/opacity)
    - Night parity: player bar visually identical with `data-theme` unset (ring-token harmonization noted if applied)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Player bar surface, border, button hover, and focus ring tokenized; recolor transition added; no new UI; night unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize LessonsModal + VideoSection (backdrop, dialog, inputs, video card + modal)</name>
  <files>src/components/LessonsModal.astro, src/components/VideoSection.astro</files>
  <read_first>
    - src/components/LessonsModal.astro (inline-style backdrop line 27 + dialog gradient line 38; scoped surfaces lines 145, 149, 165, 171, 221, 259, 265, 275, 297, 367)
    - src/components/VideoSection.astro (lines 572, 627, 641, 694, 747, 780, 835, 849, 850, 858, 873, 888 — card bg, scrim, play btn, modal backdrop, modal container, controls)
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (sections "src/components/LessonsModal.astro" and "src/components/VideoSection.astro" — full literal->token maps; LessonsModal is the canonical modal pattern VideoSection mirrors)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (Surface Inventory rows for LessonsModal backdrop/dialog/inputs/dividers and VideoSection card/modal/container)
  </read_first>
  <action>
    LessonsModal.astro: backdrop inline `rgba(0,0,0,0.72)` -> `var(--color-overlay-scrim)`; the dialog gradient over `rgba(10,8,9,0.82/0.9)` -> base it on `var(--color-bg-modal)` (the dialog bg). Because this is a `linear-gradient(...) over url(...)` inside an inline `style=`/template literal, prefer LIFTING the dialog background into a scoped CSS class so `var(--color-bg-modal)` resolves cleanly (PATTERNS flags this); if the inline `var()` resolves at runtime, inline is acceptable. Scoped surfaces: dividers `rgba(255,255,255,0.1)`/`rgba(255,255,255,0.08)`/`rgba(255,255,255,0.14)` -> `var(--color-border)`; accent divider `rgba(243,169,189,0.5)` -> `var(--color-accent-stroke)`; input field `background:#070506` -> `var(--color-surface-card)`; `color:#fff` -> `var(--color-text)` (or `var(--color-text-on-accent)` where ON the pink submit button); submit-btn `color:#000` -> `var(--color-text-on-accent)`; accent tint `rgba(243,169,189,0.14)` -> `var(--color-accent-tint-strong)`. Deep box-shadows over rgba(0,0,0,...) may remain (theme-neutral). VideoSection.astro: card bg `rgba(6,6,6,0.85)` -> `var(--color-surface-video)`; card scrim gradient over rgba(0,0,0,...) -> `var(--color-hero-gradient-*)` (map dark stop->bottom); play-btn/hover overlays `rgba(0,0,0,0.4)`/`rgba(0,0,0,0.45)` -> `var(--color-overlay-scrim)` (or keep as overlay — choose the closest existing overlay token; document if none fits); accent tint `rgba(243,169,189,0.07)` -> `var(--color-accent-tint)`; modal backdrop `rgba(0,0,0,0.8)` -> `var(--color-overlay-dark)`; modal container `#000` (lines 849, 858) -> `var(--color-bg)`; control bg `rgba(0,0,0,0.5)`/`rgba(0,0,0,0.7)` -> closest overlay token (`var(--color-overlay-scrim)` for the lighter, document choice). CRITICAL: VideoSection portals its modal to `<body>` (per STATE.md) — tokens still resolve because `html[data-theme]` is the cascade root, so no special handling needed; verify the portaled modal recolors in day mode. Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "lessons backdrop token:"; grep -c "var(--color-overlay-scrim)" src/components/LessonsModal.astro; echo "lessons input token:"; grep -c "var(--color-surface-card)" src/components/LessonsModal.astro; echo "lessons raw field bg (expect 0):"; grep -c "#070506" src/components/LessonsModal.astro; echo "video card token:"; grep -c "var(--color-surface-video)" src/components/VideoSection.astro; echo "video modal backdrop token:"; grep -c "var(--color-overlay-dark)" src/components/VideoSection.astro</automated>
  </verify>
  <acceptance_criteria>
    - LessonsModal: `var(--color-overlay-scrim)` (backdrop), `var(--color-surface-card)` (input, no raw `#070506`), `var(--color-accent-stroke)` (accent divider) all present; dialog bg derives from `var(--color-bg-modal)`
    - LessonsModal submit-btn text uses `var(--color-text-on-accent)` (no raw `color:#000`)
    - VideoSection: `var(--color-surface-video)` (card) and `var(--color-overlay-dark)` (modal backdrop) present; modal container uses `var(--color-bg)` (no raw `#000`)
    - Behavior: opening either modal in day mode shows a light backdrop + light dialog (no dark island); portaled video modal recolors
    - Night parity preserved for both modals
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Lessons + Video backdrops, dialogs, inputs, card, and portaled modal tokenized; day mode recolors them; night unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Tokenize GallerySection checkerboard + PhotoSwipe vars + caption pill</name>
  <files>src/components/GallerySection.astro</files>
  <read_first>
    - src/components/GallerySection.astro (checkerboard lines 156-160; PhotoSwipe `--pswp-*` vars lines 196-201 inside `<style is:global>`; caption pill lines 218, 221, 225)
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (section "src/components/GallerySection.astro" — checkerboard, --pswp-* mapping, caption pill; note the is:global cascade caveat)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (Surface Inventory: gallery checkerboard, PhotoSwipe bg + icon colors, caption pill)
  </read_first>
  <action>
    GallerySection.astro: checkerboard `repeating-linear-gradient(45deg, #141414 0 12px, #0a0a0a 12px 24px)` -> `#141414` becomes `var(--color-gallery-bg)`, `#0a0a0a` becomes `var(--color-gallery-bg-alt)`. PhotoSwipe theme vars inside the `<style is:global>` `.pswp` block: `--pswp-bg: #000` -> `var(--color-pswp-bg)`; `--pswp-icon-color: #f2f0ee` -> `var(--color-text)`; `--pswp-icon-color-secondary: #9a9691` -> `var(--color-text-subtle)`; `--pswp-error-text-color: #f3a9bd` -> `var(--color-accent)`. (These resolve against `html[data-theme]` because PhotoSwipe mounts at body level — confirm the lightbox bg flips when the theme is toggled while the lightbox is CLOSED; toggling WHILE open is an acceptable v1 edge case to ignore per PATTERNS.) Caption pill: `color:#f2f0ee` -> `var(--color-text)`; background `rgba(0,0,0,0.55)` -> `var(--color-overlay-scrim)`; the `box-shadow ... rgba(0,0,0,0.35)` may remain (theme-neutral shadow). Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "checkerboard token:"; grep -c "var(--color-gallery-bg)" src/components/GallerySection.astro; echo "pswp bg token:"; grep -c "var(--color-pswp-bg)" src/components/GallerySection.astro; echo "raw pswp #000 (expect 0):"; grep -c -- "--pswp-bg: #000" src/components/GallerySection.astro; echo "caption scrim token:"; grep -c "var(--color-overlay-scrim)" src/components/GallerySection.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--color-gallery-bg)" src/components/GallerySection.astro` returns >= 1 (and `--color-gallery-bg-alt` present)
    - PhotoSwipe `--pswp-bg` uses `var(--color-pswp-bg)` (no raw `--pswp-bg: #000`); icon/secondary/error vars use their theme tokens
    - Caption pill background uses `var(--color-overlay-scrim)` and text uses `var(--color-text)`
    - Behavior: in day mode the gallery checkerboard placeholders are light and the lightbox bg is the day pswp value
    - Night parity preserved; `npm run build` exits 0
  </acceptance_criteria>
  <done>Gallery checkerboard, PhotoSwipe theme vars, and caption pill tokenized; lightbox + gallery recolor in day mode; night unchanged; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits static `.astro` component styles only. No input handling, no theme-state logic, no storage, no network. The PhotoSwipe `<style is:global>` block contains only CSS variable references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | (all) | Player + modals + gallery components | accept (no surface) | Changes are CSS literal->`var()` substitutions in static templates. No trust boundary crossed, no untrusted data processed. The theme attribute driving the cascade is validated upstream in 03.1-01 (see T-03.1-01). |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept (none) | No dependencies added (UI-SPEC Registry Safety). No install tasks; legitimacy gate not applicable. |
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- Night theme preserved: with `data-theme` unset, player bar, both modals, and gallery/lightbox render visually identical to pre-phase (any single-token ring harmonization on the player documented in the SUMMARY).
- Day-mode reach: with `data-theme="day"`, the player bar, lessons modal, video modal (including the body-portaled one), gallery checkerboard, and PhotoSwipe lightbox all recolor — no dark islands.
</verification>

<success_criteria>
- All four interactive/overlay surfaces consume only `var(--color-*)` for theme-relevant colors (theme-neutral box-shadows excepted).
- No dark islands among player + modals + gallery in day mode; night look unchanged.
</success_criteria>

<output>
Create `.planning/phases/03.1-day-night-theme/03.1-03-SUMMARY.md` when done. Note: (1) whether the LessonsModal dialog gradient had to be lifted to scoped CSS for `var()` to resolve; (2) any player ring-token harmonization; (3) any VideoSection overlay literals with no exact token match and the token chosen.
</output>
