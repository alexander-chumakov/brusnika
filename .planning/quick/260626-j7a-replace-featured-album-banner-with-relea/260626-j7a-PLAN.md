---
phase: quick-260626-j7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/releases.ts
  - src/styles/global.css
  - src/components/ReleasesSection.astro
  - src/pages/index.astro
  - src/components/FeaturedAlbumSection.astro
autonomous: true
requirements: [QUICK-j7a]
must_haves:
  truths:
    - "Visitor sees a «Дискография» releases carousel where the featured-album banner used to be (between marquee and tracks)."
    - "All 18 releases render in exact source order with correct type/title/year/meta."
    - "The «Неоднозначное» card shows the featured.jpg cover; the other 17 show diagonal-striped placeholder tiles."
    - "← → arrow buttons scroll the track; the track can be dragged to scroll."
    - "Nav «релизы» link still scrolls to the section (id=\"music\" preserved)."
    - "Both day and night themes render placeholder tiles and text legibly (--rel-* tokens are theme-aware)."
    - "npm run build compiles with no errors and FeaturedAlbumSection.astro no longer exists."
  artifacts:
    - path: "src/data/releases.ts"
      provides: "Typed array of 18 releases + Release interface"
      contains: "export const releases"
    - path: "src/components/ReleasesSection.astro"
      provides: "Дискография carousel, replaces FeaturedAlbumSection"
      contains: "id=\"music\""
    - path: "src/styles/global.css"
      provides: "--rel-a/--rel-b/--rel-fg tokens in both :root and [data-theme=\"day\"]"
      contains: "--rel-a"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/ReleasesSection.astro"
      via: "import + <ReleasesSection /> between MarqueeSection and TracksSection"
      pattern: "ReleasesSection"
    - from: "src/components/ReleasesSection.astro"
      to: "src/data/releases.ts"
      via: "import { releases }"
      pattern: "from '../data/releases"
---

<objective>
Replace the single featured-album banner with a horizontally-scrolling «Дискография» releases carousel, ported from the new design variant `claude_design_releases/_x/var_carousel.dc.html`.

Purpose: Surface the full discography (18 releases, 2021–today) instead of one album, while keeping the existing visual language, scroll-reveal, day/night theming, and the `#music` nav anchor.

Output: New `src/data/releases.ts` data module, new `src/components/ReleasesSection.astro` island, three `--rel-*` theme tokens, rewired `index.astro`, and the deleted `FeaturedAlbumSection.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# The component being replaced (current conventions: scoped <style>, data-reveal, astro:assets Image, --color-*/--font-* tokens)
@src/components/FeaturedAlbumSection.astro

# Sibling section — section markup, link rendering, data import conventions
@src/components/TracksSection.astro

# Data-module pattern to mirror (typed exported const + exported interface, deferral comments on placeholder urls)
@src/data/site.ts

# Where sections are imported and ordered
@src/pages/index.astro

# THE DESIGN SOURCE — header+carousel markup lines 197-244, release data array lines 521-546
@claude_design_releases/_x/var_carousel.dc.html

<interfaces>
<!-- CRITICAL token mapping: the design file uses raw names (--accent, --text, 'Prata').
     The real project uses --color-* / --font-* tokens defined in src/styles/global.css.
     Map design tokens → project tokens EVERYWHERE in the ported markup: -->

  --accent        → var(--color-accent)
  --text          → var(--color-text)
  --text-soft     → var(--color-text-muted)
  --text-dim      → var(--color-text-dim)
  --line          → var(--color-border)
  --line-strong   → var(--color-border-strong)
  --surface       → var(--color-bg-raised)
  'Prata',serif         → var(--font-serif)
  ui-monospace,monospace → var(--font-mono)
  -- (NEW) the placeholder-tile tokens stay literal: var(--rel-a), var(--rel-b), var(--rel-fg)

<!-- Theme tokens live in src/styles/global.css, NOT Layout.astro inline.
     :root block (night) ends at the line with `--font-mono: ...;` followed by `}` (~line 84).
     :root[data-theme="day"] block (day) ends at `--color-border-strong: ...;` followed by `}` (~line 125). -->

<!-- The one cover image is already imported the same way FeaturedAlbumSection does it:
     import featuredImg from '../assets/images/featured.jpg'  +  astro:assets <Image>. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create releases.ts data module + add --rel-* theme tokens</name>
  <files>src/data/releases.ts, src/styles/global.css</files>
  <action>
Create `src/data/releases.ts` mirroring the `src/data/site.ts` pattern (header doc-comment, exported `interface`, exported typed `const` array). Define:

  export interface Release {
    type: string;      // сингл | EP | live | альбом | мини-альбом
    title: string;
    year: string;
    meta: string;      // '' when none
    live?: boolean;    // ● live badge
    cover?: boolean;   // true ONLY for «Неоднозначное» → component maps to featured.jpg
    url: string;
  }

Export `const releases: Release[]` with all 18 entries EXACTLY in source order from design lines 527–546. Every `url` is the Spotify artist page `https://open.spotify.com/artist/11epkfgLUMWmXsTbgfPu4B`. Add a header-comment note (mirroring site.ts deferral style) that per-release real links are deferred — all point to the Spotify artist page for now. The 18 entries in order:
  1. сингл «Доодури» 2026 «новый сингл · 15 мая»
  2. сингл «Весеннее танго» 2026 «кавер на Анну Герман»
  3. live «Home Session» 2026 «акустический live · апрель» (live:true)
  4. EP «Преисполненный» 2025 «EP + Deluxe»
  5. сингл «Вспомни меня» 2025 «с YERKATT»
  6. сингл «Клуб Неоправданных Надежд» 2025 «» (meta '')
  7. сингл «Научи меня быть» 2025 «» (meta '')
  8. live «Live Урбан 2024» 2025 «концертный EP» (live:true)
  9. альбом «Неоднозначное» 2024 «11 песен · дебют» (cover:true)
  10. EP «неактуальное» 2024 «мини-альбом»
  11. сингл «Безнаказанным» 2024 «» (meta '')
  12. сингл «Развлечение» 2023 «» (meta '')
  13. сингл «Пари» 2023 «» (meta '')
  14. сингл «Занавес» 2022 «» (meta '')
  15. сингл «Осколки» 2022 «» (meta '')
  16. сингл «Божья Коровка» 2022 «» (meta '')
  17. EP «босиком» 2021 «» (meta '')
  18. мини-альбом «как поймать себя (?)» 2021 «» (meta '')

Then add the three placeholder-tile tokens to `src/styles/global.css` in BOTH theme blocks (do NOT touch Layout.astro — tokens live in global.css which Layout imports):
  - In the night `:root { ... }` block (just before its closing `}`, near `--font-mono`): `--rel-a: #191919; --rel-b: #0e0e0e; --rel-fg: #efe9e0;`
  - In the `:root[data-theme="day"] { ... }` block (just before its closing `}`, after `--color-border-strong`): `--rel-a: #efe6d8; --rel-b: #e6dccb; --rel-fg: #6a5a48;`
Match the file's existing two-space indentation and grouping-comment style.
  </action>
  <verify>
    <automated>grep -c "rel(" /dev/null; node -e "const r=require('fs').readFileSync('src/data/releases.ts','utf8'); const n=(r.match(/title:/g)||[]).length; if(n!==18){console.error('expected 18 releases, got '+n);process.exit(1)}; if(!/cover:\s*true/.test(r)){console.error('missing cover release');process.exit(1)}; if((r.match(/live:\s*true/g)||[]).length!==2){console.error('expected 2 live');process.exit(1)}; console.log('releases ok: '+n)"</automated>
    <automated>grep -v '^[[:space:]]*\/' src/styles/global.css | grep -c -- '--rel-a' | grep -qx 2 && echo "rel tokens in both themes" || (echo "expected --rel-a in both :root and day blocks"; exit 1)</automated>
  </verify>
  <done>releases.ts exports 18 typed releases (1 cover, 2 live) all pointing to the Spotify artist URL; global.css defines --rel-a/--rel-b/--rel-fg in both the night and day theme blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReleasesSection.astro carousel component</name>
  <files>src/components/ReleasesSection.astro</files>
  <action>
Create `src/components/ReleasesSection.astro` porting design lines 197–244, replacing FeaturedAlbumSection. Follow FeaturedAlbumSection/TracksSection conventions: frontmatter doc-comment, `import { Image } from 'astro:assets'`, `import featuredImg from '../assets/images/featured.jpg'`, `import { releases } from '../data/releases.ts'`, scoped `<style>`, `data-reveal` wrappers, and the project `--color-*`/`--font-*` tokens (apply the token mapping from <interfaces> — do NOT leave raw `--accent`/`'Prata'` from the design file).

Markup (`<section id="music">` — keep id="music" so Nav «релизы»→#music works; add `scroll-margin-top:70px`):
- A `data-reveal` header row (flex, space-between, wrap): left = `--font-mono` eyebrow «релизы» (accent) + `--font-serif` h2 «Дискография <span> 2021 — сегодня</span>» where the span is accent + italic; right = a «слушать всё ↗» `<a>` to the Spotify artist page (`target="_blank" rel="noopener"`) plus two round 44px arrow buttons («←» / «→»).
- A `data-reveal` scroll track `<div id="rel-track" class="rel-scroll">`: display:flex, gap, overflow-x:auto, scroll-snap-type:x mandatory, scroll-padding-left, cursor:grab.
- `{releases.map((r) => ( ... ))}` rendering each card as an `<a href={r.url} target="_blank" rel="noopener" draggable="false">` (flex:0 0 auto; width clamp(208px,56vw,260px); scroll-snap-align:start):
  * Square cover `aspect-ratio:1/1`, rounded, overflow hidden.
    - If `r.cover`: render astro:assets `<Image src={featuredImg} ... format="webp" loading="lazy" />` filling the square (object-fit:cover, object-position center 42%) + a bottom dark gradient + the title overlaid bottom-left in `--font-serif` white.
    - Else: a placeholder tile = `repeating-linear-gradient(135deg, var(--rel-a) 0 15px, var(--rel-b) 15px 30px)` + a soft pink radial overlay + the title centered in `--font-serif` `var(--rel-fg)` + an 8px accent dot top-left.
    - If `r.live`: a «● live» badge top-right (`--font-mono`, pill, blurred dark bg, accent border).
  * Card footer below the square: `--font-mono` «{r.type} · {r.year}» (accent), then `--font-serif` title (`--color-text`), then a dim `--color-text-dim` meta line with `min-height` so empty-meta cards keep alignment.
- Accessibility: arrow buttons have `aria-label="назад"` / `aria-label="вперёд"`; cards are real `<a>` links with `rel="noopener"`.
- Mobile/responsive: include a scoped media query (≤860px, matching other sections) reducing section side padding and the scroll-track padding; cards already use vw-based clamp width so they remain swipeable.

Inline vanilla `<script>` (no library, no client directive needed — plain DOM script like Layout's inline scripts; guard for elements existing):
- Select `#rel-track`; if absent, return.
- Wire the two arrow buttons (give them ids or a shared class/data-attr) to `track.scrollBy({ left: dir * Math.min(track.clientWidth * 0.85, 580), behavior: 'smooth' })`.
- Drag-to-scroll via pointer events: on pointerdown record startX + scrollLeft and set cursor `grabbing`; on pointermove (while down) update `scrollLeft -= dx`; on pointerup/pointerleave restore cursor `grab`. Keep it tiny.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | grep -Ei "ReleasesSection|error" || echo "no astro-check errors for ReleasesSection"</automated>
    <automated>grep -q 'id="music"' src/components/ReleasesSection.astro && grep -q 'id="rel-track"' src/components/ReleasesSection.astro && grep -q "from '../data/releases" src/components/ReleasesSection.astro && grep -q "var(--font-serif)" src/components/ReleasesSection.astro && ! grep -q "fonts.googleapis" src/components/ReleasesSection.astro && echo "component structure ok"</automated>
  </verify>
  <done>ReleasesSection.astro renders the header + a snap-scroll track of 18 release cards from releases.ts, uses project --color-*/--font-* tokens (no Google Fonts), keeps id="music", and ships a tiny inline arrow + drag-scroll script. astro check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire index.astro, delete FeaturedAlbumSection, build</name>
  <files>src/pages/index.astro, src/components/FeaturedAlbumSection.astro</files>
  <action>
In `src/pages/index.astro`: replace `import FeaturedAlbumSection from '../components/FeaturedAlbumSection.astro';` with `import ReleasesSection from '../components/ReleasesSection.astro';`, and replace the `<FeaturedAlbumSection />` usage with `<ReleasesSection />` in the SAME position (between `<MarqueeSection />` and `<TracksSection />`). Update the header doc-comment section-order note so it reads `ReleasesSection (carousel)` instead of `FeaturedAlbum`.

Then DELETE `src/components/FeaturedAlbumSection.astro` (fully replaced). Use `git rm` or remove the file.

Finally run `npm run build` to confirm the site compiles end-to-end.
  </action>
  <verify>
    <automated>test ! -f src/components/FeaturedAlbumSection.astro && ! grep -q "FeaturedAlbumSection" src/pages/index.astro && grep -q "ReleasesSection" src/pages/index.astro && echo "wiring + delete ok"</automated>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>index.astro imports and renders ReleasesSection between marquee and tracks, the doc-comment notes the carousel, FeaturedAlbumSection.astro is deleted, and `npm run build` completes successfully.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no errors.
- `npx astro check` reports no errors in the new files.
- Manual (optional, day + night): the «Дискография» carousel appears between marquee and tracks; «Неоднозначное» shows the cover, the other 17 show striped tiles legible in both themes; arrow buttons and drag scroll the track; Nav «релизы» still jumps to it.
</verification>

<success_criteria>
- All 18 releases ported exactly (order, type, title, year, meta, live, cover) with Spotify artist URLs.
- ReleasesSection replaces FeaturedAlbumSection at the same position; `#music` anchor preserved.
- `--rel-a/--rel-b/--rel-fg` defined for both themes in global.css; no raw design tokens or Google Fonts leak into the component.
- FeaturedAlbumSection.astro deleted; `npm run build` passes.
</success_criteria>

<output>
Create `.planning/quick/260626-j7a-replace-featured-album-banner-with-relea/260626-j7a-SUMMARY.md` when done. Note in the SUMMARY that per-release Spotify/streaming links are deferred (all 18 currently point to the Spotify artist page), mirroring the existing placeholder-link tracker convention.
</output>
