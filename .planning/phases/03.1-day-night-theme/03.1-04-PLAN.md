---
phase: 03.1-day-night-theme
plan: 04
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03"]
files_modified:
  - src/components/Nav.astro
  - public/icons/theme-eye.svg
autonomous: false
requirements: [D-01, D-04]
must_haves:
  truths:
    - "The day theme is the LOCKED Warm Cream palette (D-01 resolved from the user's mockup) — no A/B comparison needed"
    - "The toggle icon is the LOCKED brand eye-with-eyelashes (D-04 resolved from the user's mockup) — no icon-variant prototyping needed"
    - "The eye toggle uses a real eye asset at public/icons/theme-eye.svg if the user supplied one, otherwise a clean inline eye-with-eyelashes SVG fallback — the toggle is never a broken <img>"
    - "The user has viewed the finished day theme + eye toggle on the live site and approved (or requested tweaks)"
    - "Implements decisions D-01 (day palette = Warm Cream, LOCKED from mockup), D-02 (deeper day accent #c95572, LOCKED), and D-04 (brand eye-with-eyelashes toggle icon that blinks on toggle, LOCKED)"
  artifacts:
    - path: "public/icons/theme-eye.svg"
      provides: "Brand eye-with-eyelashes toggle asset (user-supplied artwork or clean hand-authored fallback)"
      contains: "<svg"
  key_links:
    - from: "Nav.astro toggle button (.theme-eye)"
      to: "the locked brand eye artwork"
      via: "inline SVG or <img src=/icons/theme-eye.svg>"
      pattern: "theme-eye"
---

<objective>
Finalize the two formerly-deferred design decisions that are now LOCKED by the user's mockup (`Внимание Брусника-2.html`): the day palette is **Warm Cream** (D-01 / D-02) and the toggle icon is the **brand eye with eyelashes** (D-04). Both were applied directly in Plans 01/03 — there is no longer an A/B palette comparison or icon-variant prototyping. This plan's remaining work is narrow: reconcile the eye asset (real artwork if the user dropped one in, otherwise the inline fallback authored in Plan 01) at `public/icons/theme-eye.svg`, and run a light human-confirmation checkpoint where the user looks at the finished day theme + eye toggle on the live site and approves or requests tweaks.

Purpose: D-01 and D-04 were originally "show two, user picks by sight." The user generated an authoritative mockup that settled both, so the heavy comparison machinery is gone. What remains is making sure the locked design is in place and lightly blessed by the user, plus guaranteeing the eye toggle is never a broken `<img>` (the mockup referenced the eye art by a broken UUID — the artwork does not exist yet).

Output: `public/icons/theme-eye.svg` present (real or fallback) and wired as the toggle icon; user confirmation of the finished day theme + eye toggle recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-day-night-theme/03.1-CONTEXT.md
@.planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md
@.planning/phases/03.1-day-night-theme/03.1-01-PLAN.md
@.planning/phases/03.1-day-night-theme/03.1-02-PLAN.md
@.planning/phases/03.1-day-night-theme/03.1-03-PLAN.md
</context>

<tasks>

<task type="auto" gate="blocking-human">
  <name>Task 1: [BLOCKING] Reconcile the eye toggle asset — real artwork or inline fallback at public/icons/theme-eye.svg</name>
  <files>public/icons/theme-eye.svg, src/components/Nav.astro</files>
  <read_first>
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (section "Toggle Control" -> "Form (LOCKED, D-04): brand eye with eyelashes" -> "Asset reality — [BLOCKING]")
    - src/components/Nav.astro (the eye toggle button + inline `.theme-eye` SVG fallback added in Plan 01/03)
    - .planning/phases/03.1-day-night-theme/03.1-CONTEXT.md (D-04 — user wants a custom brand toggle icon)
  </read_first>
  <action>
    [BLOCKING — autonomous:false] The real eye artwork may or may not exist yet (the user's mockup referenced it by a broken UUID `<img src>`; nothing was embedded). FIRST check `public/icons/theme-eye.svg`:
    (a) IF the user has placed a real eye-with-eyelashes artwork at `public/icons/theme-eye.svg`, use it as the toggle icon. Verify it is an inert static SVG (no `<script>`, no external `href`, no event handlers — pure `<svg>`/`<path>` markup) before wiring. Wire it into the `[data-theme-toggle]` button in Nav.astro (inline SVG preferred so `currentColor` + the `.theme-eye` blink class apply; or `<img src="/icons/theme-eye.svg" class="theme-eye">` if the artwork must stay external). Keep the `.theme-eye` class so the Plan 01 blink keyframes still fire.
    (b) IF no real asset is present, save the clean inline eye-with-eyelashes SVG that Plan 01/03 authored into `public/icons/theme-eye.svg` (a single inert `<svg>` with the eye + eyelashes paths, 20px-ish viewBox, `currentColor`-friendly) so the asset path exists and is reusable, AND keep the inline `.theme-eye` SVG live in Nav.astro as the rendered icon. Either way the toggle MUST render a working eye — never a broken `<img>`.
    Because this requires knowing whether the user dropped in real artwork (a human-only fact), this task is gated `blocking-human`: pause and ask the user "Have you placed the real eye artwork at public/icons/theme-eye.svg, or should I keep the hand-authored fallback?" before finalizing. Implements the asset-reality half of D-04. Do NOT alter the LOCKED Warm Cream palette (it is final in Plan 01) — this plan no longer touches palette values.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "eye asset exists:"; ls public/icons/theme-eye.svg 2>&1 | grep -c "theme-eye.svg"; echo "valid svg root:"; grep -c "<svg" public/icons/theme-eye.svg; echo "no script in svg (expect 0):"; grep -ci "<script" public/icons/theme-eye.svg; echo "eye wired in nav:"; grep -c "theme-eye" src/components/Nav.astro</automated>
  </verify>
  <acceptance_criteria>
    - `public/icons/theme-eye.svg` exists and is a valid inert SVG (`<svg` present, no `<script>`, no external `href`, no event handlers)
    - The toggle button renders a working eye-with-eyelashes icon (inline SVG or `<img>` to the asset) — never a broken `<img>`
    - The `.theme-eye` class is present so the Plan 01 blink animation fires on toggle
    - User has confirmed whether the real artwork is supplied or the fallback should stand (blocking-human resolved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>`public/icons/theme-eye.svg` exists (real user artwork or hand-authored fallback) and is the live toggle icon with the `.theme-eye` blink class; no broken image; build passes; palette untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms the finished day theme + eye toggle</name>
  <files>(none — confirmation checkpoint, no files changed)</files>
  <action>Pause for the user. Do NOT edit any files in this task. Present the finished, locked day/night theme on the live site per how-to-verify below, and let the user approve or request tweaks. This is a LIGHT confirmation — the palette (Warm Cream) and icon (brand eye) are already LOCKED by the user's mockup; there is no A/B choice to make here. The user is just signing off that the finished result on the live site matches their intent.</action>
  <what-built>
    The full day/night theme is live and switches end-to-end with the LOCKED Warm Cream day palette (#f7f4ef background, #c95572 accent) and the LOCKED brand eye-with-eyelashes toggle icon (which blinks on toggle, and — where supported and not reduced-motion — reveals the new theme with a circular wipe from the toggle).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or open the deployed preview) and open the site.
    2. Click the top-right eye toggle to enter day mode. Confirm the Warm Cream look (warm off-white background, deeper pink accent) across hero, shows, gallery, the now-playing player bar, and an open lessons/video modal.
    3. Confirm the eye icon blinks on each toggle, and that the optional circular reveal (if your browser supports `startViewTransition` and you don't have reduced-motion on) looks right — or is gracefully absent.
    4. Toggle back to night and confirm it looks exactly like the existing dark site (unchanged).
    5. Reply "approved" if it matches your mockup, or describe any tweaks (e.g. a specific surface color or the eye styling).
  </how-to-verify>
  <verify><human-check>User has viewed the finished day theme + eye toggle on the live site and replied "approved" or listed tweaks.</human-check></verify>
  <resume-signal>Reply "approved" or describe any tweaks to the day theme / eye toggle.</resume-signal>
  <done>User has confirmed the finished locked day theme + eye toggle (or provided a tweak list for follow-up).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static SVG asset | A hand-authored or user-supplied SVG file served from `public/icons/theme-eye.svg`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06 | Tampering / XSS | `public/icons/theme-eye.svg` (and inline eye SVG in Nav) | mitigate | SVG must be inert: no embedded `<script>`, no external `href`, no event handlers — pure `<svg>`/`<path>` markup. Verify a user-supplied asset is inert before wiring (Task 1 grep gate). If inlined into Nav, ensure no untrusted interpolation. |
| T-03.1-07 | (config) | LOCKED `[data-theme="day"]` palette | accept (no surface) | Palette values are static CSS literals already committed in Plan 01; no runtime input. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept (none) | No dependencies added (UI-SPEC Registry Safety). No install tasks; legitimacy gate not applicable. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `public/icons/theme-eye.svg` exists and is a valid inert SVG; the toggle renders a working eye (never broken).
- The day theme is the LOCKED Warm Cream palette (no `[data-theme="day-a"]` prototype anywhere; no icon-variant switch).
- Night theme pixel-identical to pre-phase.
- User has confirmed the finished result on the live site.
</verification>

<success_criteria>
- D-01 (Warm Cream) and D-04 (brand eye) are reflected in the finished, live theme — both LOCKED, no comparison step.
- The eye toggle asset exists at `public/icons/theme-eye.svg` (real or fallback) and is wired with the blink class; never a broken image.
- The user has viewed and approved (or tweak-listed) the finished day theme + eye toggle; build passes; night unchanged.
</success_criteria>

<output>
Create `.planning/phases/03.1-day-night-theme/03.1-04-SUMMARY.md` when done. Record: whether the real eye artwork was supplied or the fallback stands, the user's approval (or tweak list), and confirmation that night is unchanged.
</output>
