---
phase: 03.1-day-night-theme
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/components/HeroSection.astro
  - src/components/FeaturedAlbumSection.astro
  - src/components/ShowsSection.astro
  - src/components/SingSection.astro
  - src/components/MarqueeSection.astro
  - src/components/TracksSection.astro
  - src/components/AboutSection.astro
  - src/components/FooterSection.astro
autonomous: true
requirements: [D-06, UISPEC-STEP-1]
must_haves:
  truths:
    - "In day mode, hero/shows/sing/featured/marquee/footer sections are fully recolored (no dark islands)"
    - "Section image scrims, dividers, ticket buttons, and text-on-accent all flip with the theme"
    - "Night theme for these sections is pixel-identical to before (literals replaced with their own night-valued tokens)"
  artifacts:
    - path: "src/components/HeroSection.astro"
      provides: "Tokenized image scrims, tagline gradient accent midpoint, text colors"
      contains: "var(--color-hero-gradient"
    - path: "src/components/ShowsSection.astro"
      provides: "Tokenized gradient, dividers, ticket button bg + border"
      contains: "var(--color-accent-ticket"
    - path: "src/components/FooterSection.astro"
      provides: "Tokenized divider + accent glow"
      contains: "var(--color-border-subtle)"
  key_links:
    - from: "these components"
      to: "global.css :root + [data-theme=day] tokens (defined in 03.1-01)"
      via: "var(--color-*) consumption"
      pattern: "var\\(--color-"
---

<objective>
Tokenize the "light" (non-modal, non-player) section components so they recolor fully in day mode: HeroSection, FeaturedAlbumSection, ShowsSection, SingSection, MarqueeSection, TracksSection, AboutSection, FooterSection. Every hardcoded hex/rgba in these files is replaced with the matching `var(--token)` already defined by Plan 01. No tokens are defined here — only consumed.

Purpose: Delivers the D-06 "full theming, no dark islands" requirement for the static content sections. These files have zero overlap with Plan 03's files, so the two run in parallel in Wave 2.

Output: Eight section components fully token-driven; night theme unchanged; day mode recolors them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md
@.planning/phases/03.1-day-night-theme/03.1-PATTERNS.md
@.planning/phases/03.1-day-night-theme/03.1-01-PLAN.md

<interfaces>
<!-- Tokens are DEFINED in 03.1-01 (src/styles/global.css). This plan only consumes them. -->
<!-- Canonical name/night/day values live in 03.1-UI-SPEC.md "Complete Token Contract". Key ones used here: -->
- --color-hero-gradient-top / --color-hero-gradient-bottom  (image + section scrims)
- --color-bg-raised  (Marquee + Sing section backgrounds)
- --color-border  (row/section dividers, default 0.08)
- --color-accent  (#f3a9bd night / #c94a6e day — Hero tagline gradient midpoint, Marquee/Footer accent)
- --color-accent-stroke  (Marquee -webkit-text-stroke)
- --color-accent-ticket / --color-accent-ticket-border  (Shows ticket buttons)
- --color-text / --color-text-muted  (headings, body, descriptions)
- --color-text-on-accent  (#000 night / #fff day — text sitting ON pink accent)
- --color-border-subtle  (Footer divider)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize HeroSection + FeaturedAlbumSection (gradient-overlay siblings)</name>
  <files>src/components/HeroSection.astro, src/components/FeaturedAlbumSection.astro</files>
  <read_first>
    - src/components/HeroSection.astro (image scrim gradients lines 104-117; tagline colors/gradient lines 158, 164, 188, 212)
    - src/components/FeaturedAlbumSection.astro (shadow line 59; horizontal gradient line 72; text colors lines 103, 109, 127, 141)
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (sections "src/components/HeroSection.astro" and "src/components/FeaturedAlbumSection.astro" — line-by-line literal->token map)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (section "Theming Scope — Surface Inventory"; "Accent Reserved For" — Hero tagline gradient uses accent midpoint)
  </read_first>
  <action>
    HeroSection.astro: image scrim `rgba(0,0,0,0.34)` -> `var(--color-hero-gradient-top)`, `rgba(0,0,0,0.8)` -> `var(--color-hero-gradient-bottom)` (the mid stop rgba(0,0,0,0.22) may stay as-is or interpolate — keep visual parity by leaving intermediate stops if they have no token, but the top/bottom anchors MUST tokenize). Tagline: `color:#f3a9bd` -> `var(--color-accent)`; the Prata text gradient `linear-gradient(176deg, #ffe7ed, #f3a9bd, #dd7791)` -> replace the MIDPOINT `#f3a9bd` with `var(--color-accent)` (endpoints are discretion — keep existing for night parity); `color:#cfcbc6` -> `var(--color-text-muted)`; any `color:#000` sitting on accent -> `var(--color-text-on-accent)`. FeaturedAlbumSection.astro: horizontal overlay `rgba(0,0,0,0.85)`/`rgba(0,0,0,0.5)` -> `var(--color-hero-gradient-bottom)`/`var(--color-hero-gradient-top)` (map darkest->bottom, lighter->top to preserve night look); `color:#fff` -> `var(--color-text)`; `color:#d8d4cf` and `color:#cfcbc6` -> `var(--color-text-muted)`; `color:#000` on accent -> `var(--color-text-on-accent)`. Box-shadows over rgba(0,0,0,...) may remain (shadows are theme-neutral; discretion — leave for night parity). Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "hero gradient token:"; grep -c "var(--color-hero-gradient" src/components/HeroSection.astro; echo "hero raw accent literal #f3a9bd (expect 0):"; grep -c "#f3a9bd" src/components/HeroSection.astro; echo "featured text token:"; grep -c "var(--color-text" src/components/FeaturedAlbumSection.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--color-hero-gradient" src/components/HeroSection.astro` returns >= 2
    - `grep -c "#f3a9bd" src/components/HeroSection.astro` returns 0 (the accent literal is now `var(--color-accent)`)
    - FeaturedAlbumSection: no raw `color:#fff` / `color:#000` text literals remain (all tokenized)
    - Night theme parity: with `data-theme` unset, both sections render visually identical to pre-phase
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Hero + Featured scrims, tagline accent, and text colors tokenized; night look unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize ShowsSection + SingSection (sibling gradient/divider/row pattern)</name>
  <files>src/components/ShowsSection.astro, src/components/SingSection.astro</files>
  <read_first>
    - src/components/ShowsSection.astro (gradient line 112; text line 122; dividers lines 137, 141; ticket button lines 172, 173)
    - src/components/SingSection.astro (section bg line 64; gradient line 97; dividers lines 144, 148; text-on-accent line 179)
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (section "ShowsSection.astro + SingSection.astro" — they mirror each other)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (Surface Inventory: ShowsSection gradient/dividers/ticket; SingSection bg/dividers)
  </read_first>
  <action>
    ShowsSection.astro: section gradient `rgba(0,0,0,0.05)`/`rgba(0,0,0,0.55)` -> `var(--color-hero-gradient-top)`/`var(--color-hero-gradient-bottom)`; `color:#fff` -> `var(--color-text)`; `border-top: 1px solid rgba(255,255,255,0.1)` -> `var(--color-border)`; ticket button border `rgba(243,169,189,0.6)` -> `var(--color-accent-ticket-border)`; ticket button background `rgba(243,169,189,0.08)` -> `var(--color-accent-ticket)`. SingSection.astro: section background `#050505` -> `var(--color-bg-raised)`; gradient `rgba(0,0,0,0.05)`/`rgba(0,0,0,0.45)` -> `var(--color-hero-gradient-top)`/`var(--color-hero-gradient-bottom)`; `border-top: 1px solid rgba(255,255,255,0.1)` -> `var(--color-border)`; `color:#000` (on accent) -> `var(--color-text-on-accent)`. Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "shows ticket token:"; grep -c "var(--color-accent-ticket" src/components/ShowsSection.astro; echo "shows raw white divider (expect 0):"; grep -Ec "rgba\(255, ?255, ?255, ?0.1\)" src/components/ShowsSection.astro; echo "sing bg token:"; grep -c "var(--color-bg-raised)" src/components/SingSection.astro; echo "sing raw #050505 (expect 0):"; grep -c "#050505" src/components/SingSection.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--color-accent-ticket" src/components/ShowsSection.astro` returns >= 2 (bg + border)
    - ShowsSection has no raw `rgba(255,255,255,0.1)` divider literals
    - `grep -c "var(--color-bg-raised)" src/components/SingSection.astro` returns >= 1 and raw `#050505` returns 0
    - SingSection text-on-accent uses `var(--color-text-on-accent)` (no raw `color:#000`)
    - Night parity preserved for both; `npm run build` exits 0
  </acceptance_criteria>
  <done>Shows + Sing gradients, dividers, ticket buttons, section bg, and text-on-accent tokenized; night unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Tokenize MarqueeSection + TracksSection + AboutSection + FooterSection</name>
  <files>src/components/MarqueeSection.astro, src/components/TracksSection.astro, src/components/AboutSection.astro, src/components/FooterSection.astro</files>
  <read_first>
    - src/components/MarqueeSection.astro (lines 27-50: border-top, bg #040404, text + -webkit-text-stroke rgba(243,169,189,0.45))
    - src/components/TracksSection.astro (line 106 row divider rgba(255,255,255,0.2))
    - src/components/AboutSection.astro (already token-driven — audit for stray literals only)
    - src/components/FooterSection.astro (line 118 email/dot glow rgba(243,169,189,0.9); line 158 divider rgba(255,255,255,0.06))
    - .planning/phases/03.1-day-night-theme/03.1-PATTERNS.md (sections "MarqueeSection.astro", "TracksSection.astro + AboutSection.astro", "FooterSection.astro")
  </read_first>
  <action>
    MarqueeSection.astro: `border-top rgba(255,255,255,0.07)` -> `var(--color-border)`; background `#040404` -> `var(--color-bg-raised)` (UI-SPEC raised night value is #070707 — confirm which token: per Surface Inventory MarqueeSection bg maps to `--color-bg-raised`; the source literal is #040404, so use `var(--color-bg-raised)` and accept the tiny night-value shift OR, to keep strict night parity, note the discrepancy in the SUMMARY. Prefer `var(--color-bg-raised)` for theming reach — record the 0.x% night delta if any); the `@supports`-gated `-webkit-text-stroke: 1px rgba(243,169,189,0.45)` and matching `color: rgba(243,169,189,0.45)` fallback -> `var(--color-accent-stroke)`. TracksSection.astro: row divider `rgba(255,255,255,0.2)` -> `var(--color-border)` (NOTE: 0.2 is stronger than the 0.08 default border token; this is a deliberate near-parity simplification per PATTERNS — if the divider visibly weakens in night mode, instead leave it and note it; prefer tokenization unless it visibly degrades). AboutSection.astro: audit — replace any stray literal found; expected 0 changes (already token-driven). FooterSection.astro: divider `rgba(255,255,255,0.06)` -> `var(--color-border-subtle)`; email/dot glow `box-shadow ... rgba(243,169,189,0.9)` -> keep accent-derived using `var(--color-accent)` for the glow color (so it deepens to #c94a6e in day) — confirm the glow still reads on light bg. Implements D-06 + UISPEC-STEP-1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "marquee stroke token:"; grep -c "var(--color-accent-stroke)" src/components/MarqueeSection.astro; echo "marquee raw bg #040404 (expect 0):"; grep -c "#040404" src/components/MarqueeSection.astro; echo "footer subtle divider token:"; grep -c "var(--color-border-subtle)" src/components/FooterSection.astro; echo "tracks raw white divider (expect 0):"; grep -Ec "rgba\(255, ?255, ?255, ?0.2\)" src/components/TracksSection.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--color-accent-stroke)" src/components/MarqueeSection.astro` returns >= 1 (covers stroke + fallback color) and raw `#040404` returns 0
    - `grep -c "var(--color-border-subtle)" src/components/FooterSection.astro` returns >= 1 (divider) and Footer glow uses `var(--color-accent)`
    - TracksSection row divider tokenized (no raw `rgba(255,255,255,0.2)`) OR the parity exception is documented in the SUMMARY
    - AboutSection has zero remaining raw color literals
    - Night parity preserved (any intentional night-value delta on Marquee bg / Tracks divider documented); `npm run build` exits 0
  </acceptance_criteria>
  <done>Marquee bg/stroke, Tracks divider, About (verified clean), and Footer divider+glow tokenized; night look preserved or deltas documented; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only edits static `.astro` component styles/markup — no input handling, no scripts, no storage, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04 | (all) | Static section components | accept (no surface) | Changes are pure CSS literal->`var()` substitutions in static templates. No trust boundary is crossed, no untrusted data is processed, no new code path is introduced. The theme attribute consumed via CSS cascade is set/validated upstream in 03.1-01 (see T-03.1-01). |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept (none) | No dependencies added (UI-SPEC Registry Safety). No install tasks; legitimacy gate not applicable. |
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- Night theme preserved: with `data-theme` unset, all eight sections render visually identical to pre-phase (any deliberate token-value delta on Marquee bg / Tracks divider documented in the SUMMARY).
- Day-mode reach: with `data-theme="day"` set, none of these eight sections show a dark "island" — all recolor via the cascade.
</verification>

<success_criteria>
- All eight static section components consume only `var(--color-*)` for theme-relevant colors (no raw theme literals, aside from documented parity exceptions and theme-neutral box-shadows).
- Night look unchanged; day look fully recolored (no dark islands among these sections).
</success_criteria>

<output>
Create `.planning/phases/03.1-day-night-theme/03.1-02-SUMMARY.md` when done. Note any night-value deltas accepted (Marquee #040404->bg-raised, Tracks 0.2->border) and the Footer glow accent decision.
</output>
