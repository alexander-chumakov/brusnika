---
phase: 04-test-deploy-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-test-deploy-verification/04-VERIFICATION.md
autonomous: false
requirements: [AUD-05, BOOK-02]
user_setup: []

must_haves:
  truths:
    - "D-03: On a real iPhone in Safari, scrubbing to mid-track plays from that position without restarting (the device half of SC3 / AUD-05) — one of the two human-only checks"
    - "D-06: A live booking submission reaches Соня's Telegram correctly formatted in Russian and the band confirms receipt (the human half of SC5 / BOOK-02); Telegram creds confirmed configured so this is performed for real, not deferred"
    - "The verification doc's two ⏳ pending-human rows are resolved to ✅ or ⛔ with the user's reported result"
  artifacts:
    - path: ".planning/phases/04-test-deploy-verification/04-VERIFICATION.md"
      provides: "Updated SC3-device and SC5-receipt rows with the human verification outcome"
      contains: "SC3"
  key_links:
    - from: "user's iPhone Safari + the band's Telegram"
      to: ".planning/phases/04-test-deploy-verification/04-VERIFICATION.md"
      via: "user reports result; Claude records ✅/⛔ with the reported proof"
      pattern: "vnimanie-brusnika.vercel.app"
---

<objective>
D-03: Resolve the two checks that physically cannot be automated and stay with the user — real-device iOS audio seek (SC3 device half, AUD-05) and live booking delivery confirmed by the band in Telegram (SC5, BOOK-02). The user performs each check using the plain-language Russian instructions written into the doc in Plan 01, reports back, and Claude records the outcome.

Purpose: Close the phase with the music-plays and booking-reaches-Соня guarantees confirmed on a real device, per the project's core value.
Output: `04-VERIFICATION.md` updated — the two ⏳ rows become ✅ (or ⛔ with the reported issue).

Decision coverage in this plan: D-03 (the two human-only checks) and D-06 (Telegram credentials confirmed configured, so the SC5 booking test is performed for real, not deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-test-deploy-verification/04-CONTEXT.md
@.planning/phases/04-test-deploy-verification/04-VERIFICATION.md

<facts>
- Live deploy URL: https://vnimanie-brusnika.vercel.app
- D-06: Telegram bot token + Соня's chat id are confirmed configured in Vercel, so the live booking test (SC5) IS performed for real in this phase — not deferred.
- D-03 locks exactly two human checks: iPhone audio seek, and band confirmation of Telegram receipt. No other human check is in scope.
- Plan 01 already wrote the Russian step-by-step instructions for both checks into 04-VERIFICATION.md ("Что проверить на телефоне").
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: iPhone Safari audio-seek verification (SC3 device half)</name>
  <files>.planning/phases/04-test-deploy-verification/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-test-deploy-verification/04-VERIFICATION.md (the "Что проверить на телефоне" audio-seek instruction block written in Plan 01)
  </read_first>
  <what-built>
    D-03: this is one of the two human-only checks that cannot be automated. Plan 01 confirmed the server side of SC3 (the CDN audio URL returns Accept-Ranges: bytes and HTTP 206 + Content-Range on a ranged request); this task confirms the remaining device-side behavior that only a real iPhone can prove.
  </what-built>
  <how-to-verify>
    На айфоне в Safari:
    1. Открой https://vnimanie-brusnika.vercel.app
    2. Нажми «слушать» на любом треке — должна заиграть музыка и появиться полоска внизу.
    3. Перемотай ползунок воспроизведения примерно на середину трека.
    4. Ожидаемо: звук продолжается с середины, НЕ начинается заново с начала.
    Сообщи: играет с середины (всё ок) — или перезапускается с начала (проблема).
  </how-to-verify>
  <action>
    When the user reports the result, update 04-VERIFICATION.md: set the SC3-device row to ✅ with the user's reported proof ("plays from mid-track on iPhone Safari, no restart") if it worked, or ⛔ with the exact reported behavior if it restarted. Do not change the already-✅ SC3 server-side row.
  </action>
  <acceptance_criteria>
    - (D-03) User has reported the on-device seek behavior.
    - The SC3-device row in 04-VERIFICATION.md is resolved to ✅ (with "plays from mid-track, no restart" proof) or ⛔ (with the reported failing behavior). No longer ⏳.
  </acceptance_criteria>
  <resume-signal>Reply "играет с середины" / "approved", or describe what happened (e.g. "перезапускается с начала").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live booking → band confirms Telegram receipt (SC5)</name>
  <files>.planning/phases/04-test-deploy-verification/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-test-deploy-verification/04-VERIFICATION.md (the booking-receipt instruction block written in Plan 01)
    - src/pages/api/book.ts (the Russian message format delivered: «Новая заявка на занятие» + Формат/Имя/Контакт/Опыт)
  </read_first>
  <what-built>
    D-03 + D-06: this is the second human-only check. Plan 01 confirmed /api/book is deployed and reachable (honeypot probe returned 200) and that the bot token is absent from the client bundle. D-06: Telegram credentials are confirmed configured in Vercel, so this task performs an ACTUAL submission for real (not deferred) and confirms it is delivered to Соня's Telegram, correctly formatted in Russian.
  </what-built>
  <how-to-verify>
    На сайте https://vnimanie-brusnika.vercel.app:
    1. Открой форму записи на занятие («пойте с нами» / записаться).
    2. Заполни поля (имя, контакт, формат, опыт) тестовыми данными — без настоящих личных данных.
    3. Отправь форму. На сайте должно появиться «Спасибо!».
    4. Пусть Соня (или тот, у кого настроен бот) проверит Telegram: сообщение должно прийти за несколько секунд, на русском, с полями Формат / Имя / Контакт / Опыт.
    Сообщи: пришло и правильно оформлено — или не пришло / оформлено неверно.
  </how-to-verify>
  <action>
    D-06: When the user/band reports the result, update 04-VERIFICATION.md: set the SC5-receipt row to ✅ with the reported proof ("message delivered to Соня's Telegram, correctly formatted in Russian, within seconds") if it arrived correctly, or ⛔ with the exact reported problem if it did not arrive or was malformed. Do not record any submitted personal data, bot token, or chat id in the doc — only the pass/fail outcome.
  </action>
  <acceptance_criteria>
    - (D-06) The band/user has confirmed whether the real submission arrived in Соня's Telegram, correctly formatted in Russian.
    - The SC5-receipt row in 04-VERIFICATION.md is resolved to ✅ (with the delivery-confirmed proof) or ⛔ (with the reported failure). No longer ⏳.
    - No secret value or submitted PII is written into the doc.
  </acceptance_criteria>
  <resume-signal>Reply "пришло" / "approved", or describe the problem (e.g. "не пришло" / "поля перепутаны").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/book | The SC5 test sends a real submission across this boundary into Telegram |
| verification doc → planning repo | The doc must not capture submitted PII or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | 04-VERIFICATION.md | mitigate | The SC5 test uses placeholder test data (not real PII); only the pass/fail outcome is recorded — never the submitted fields, bot token, or chat id |
| T-04-06 | Information Disclosure | Telegram bot token at submit time | accept | Token stays server-side in Vercel (verified absent from client in Plan 01); the live submission does not expose it to the browser |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan only exercises the existing endpoint once and edits a markdown doc |

No `high`-severity threats. This plan performs two human checks against an already-deployed surface and records only outcomes; the one live submission uses test data and the honeypot/token mitigations from Plan 01 still hold.
</threat_model>

<verification>
- SC3-device row resolved to ✅/⛔ with the iPhone seek outcome.
- SC5-receipt row resolved to ✅/⛔ with the band's Telegram confirmation.
- No PII or secret recorded in the doc.
</verification>

<success_criteria>
- Both ⏳ pending-human rows in 04-VERIFICATION.md are resolved with the user's reported result (D-03).
- The real live booking test was performed and confirmed (D-06).
- The phase's full 5-criteria status report is complete and band-shareable.
</success_criteria>

<output>
Create `.planning/phases/04-test-deploy-verification/04-02-SUMMARY.md` when done.
</output>
