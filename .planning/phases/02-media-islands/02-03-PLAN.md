---
phase: 02-media-islands
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/GallerySection.astro
  - src/content/gallery/01-studio.json
  - src/content/gallery/02-guitar.json
  - src/content/gallery/03-band-guitars.json
  - src/content/gallery/04-couch.json
  - src/content/gallery/05-live.json
autonomous: false
requirements: [GAL-01, GAL-02, GAL-03]

must_haves:
  truths:
    - "Clicking a gallery photo opens it fullscreen in a dark lightbox over the existing static grid (D-17)"
    - "Visitor can advance/go back with arrow buttons, keyboard left/right, and mobile swipe, and an image counter is always shown e.g. '3 / 5' (D-17)"
    - "Pressing Escape, clicking ×, or tapping outside closes the lightbox (D-17)"
    - "A short per-photo caption appears only when the photo has one; otherwise the lightbox stays clean with counter only (D-18)"
  artifacts:
    - path: "src/components/GallerySection.astro"
      provides: "Anchor-wrapped gallery grid + PhotoSwipe 5 lightbox script + dark/pink theming"
      contains: "PhotoSwipeLightbox"
  key_links:
    - from: "src/components/GallerySection.astro"
      to: "photoswipe (npm)"
      via: "bundled <script> importing photoswipe/lightbox + photoswipe/style.css"
      pattern: "from 'photoswipe/lightbox'"
    - from: "gallery grid anchors"
      to: "PhotoSwipe"
      via: "#gallery-grid selector + children 'a' with data-pswp-width/height"
      pattern: "data-pswp-width"
---

<objective>
Make the existing static gallery grid open photos fullscreen. Wrap each gallery item in an `<a>` carrying the full-resolution image src and `data-pswp-width`/`data-pswp-height`, add `id="gallery-grid"`, and initialize PhotoSwipe 5 in a STANDARD bundled `<script>` block (the Vercel/Astro is:inline pitfall fix). Theme PhotoSwipe to the dark/pink palette, wire the optional D-18 caption through `data-pswp-caption` + a hidden caption element, and add a couple of real captions to gallery JSON so the feature is demonstrably working end-to-end.

After this plan, a visitor can click any of the 5 photos, view it fullscreen, navigate with arrows/keyboard/swipe, see a position counter, read a caption where one exists, and close it three ways — the complete "browse concert photos in fullscreen" capability.

Purpose: Deliver GAL-01..03 end-to-end with PhotoSwipe 5 (already installed, v5.4.4).
Output: GallerySection.astro upgraded with lightbox; 1-2 gallery JSON entries gain a real caption to exercise D-18.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-media-islands/02-RESEARCH.md
@.planning/phases/02-media-islands/02-PATTERNS.md
@.planning/phases/02-media-islands/02-UI-SPEC.md

<interfaces>
<!-- Existing gallery grid the lightbox attaches to. From src/components/GallerySection.astro. -->
GallerySection.astro current structure:
- `import { getCollection }` + `import { Image }` from astro:assets
- `const photos = (await getCollection('gallery')).sort(...order)`
- `galleryImages = import.meta.glob<{ default: ImageMetadata }>('../assets/images/*.jpg', { eager: true })` — imgSrc has .src, .width, .height (full-res dimensions)
- grid: `<div class="gallery-grid" data-reveal>` (NO id yet — must add `id="gallery-grid"`)
- each item: `<div class="gallery-item[ gallery-item--tall]">` wrapping `<Image .../>` — must become `<a ...>`

Gallery schema (after 02-01): { filename, alt, gridSpan: 'single'|'tall', order, caption?: string } — caption field added in 02-01.

PhotoSwipe 5.4.4 is in package.json. Import path: `photoswipe/lightbox` (PhotoSwipeLightbox) + `photoswipe/style.css`; dynamic `pswpModule: () => import('photoswipe')`.

Design tokens (global.css): --color-bg #000, --color-accent #f3a9bd, --color-text #f2f0ee, --color-text-subtle #9a9691, --color-text-dim #7d7974, --color-text-muted #bdb9b4, --font-sans, --font-mono.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Anchor-wrap the grid and initialize PhotoSwipe 5 with dark/pink theming + caption support</name>
  <read_first>
    - src/components/GallerySection.astro (full file: grid markup lines 35-59, scoped style lines 62-146)
    - .planning/phases/02-media-islands/02-PATTERNS.md (GallerySection.astro modification section: anchor wrapping, script block, theming, #gallery-grid id)
    - .planning/phases/02-media-islands/02-RESEARCH.md (Pattern 4 PhotoSwipe integration; Pitfall 1 CSS-not-loading / is:inline; Pitfall 4 data-pswp dimensions; GitHub #11035)
    - .planning/phases/02-media-islands/02-UI-SPEC.md (C-03 lightbox: counter format, caption, close interactions, navigation, theming variables, accessibility)
    - src/styles/global.css (design tokens)
  </read_first>
  <action>
In `src/components/GallerySection.astro`:

(1) Add `id="gallery-grid"` to the existing `<div class="gallery-grid" data-reveal>` — PhotoSwipe's `gallery` selector depends on it.

(2) Change each grid item from `<div class="gallery-item…">` to `<a class="gallery-item…">` with attributes: `href={imgSrc?.src ?? '#'}`, `data-pswp-width={imgSrc?.width}`, `data-pswp-height={imgSrc?.height}` (use the full-res ImageMetadata dimensions, NOT thumbnail — Pitfall 4), and `data-pswp-caption={photo.data.caption}` (omit/empty when absent). Keep the existing `gallery-item--tall` conditional class. Inside the anchor keep the existing `<Image .../>`, and when `photo.data.caption` exists, render a hidden `<span class="hidden-caption-content">{photo.data.caption}</span>` for the PhotoSwipe caption plugin. Close the tag as `</a>`. Add `text-decoration: none;` to the `.gallery-item` rule so the anchor inherits no underline.

(3) Add a STANDARD `<script>` block (NO `is:inline` — Pitfall 1 / #11035) at the end of the component that: `import PhotoSwipeLightbox from 'photoswipe/lightbox';` and `import 'photoswipe/style.css';`, constructs `new PhotoSwipeLightbox({ gallery: '#gallery-grid', children: 'a', pswpModule: () => import('photoswipe'), arrowKeys: true, escKey: true })`, registers the custom caption element via `lightbox.on('uiRegister', ...)` reading `.hidden-caption-content` on `currSlide.data.element` on the `change` event (per RESEARCH Pattern 4 / UI-SPEC), and calls `lightbox.init()`. PhotoSwipe 5 provides arrow buttons, keyboard nav, swipe, the "N / M" counter, ×-close, click-outside-close, and Escape natively — do NOT hand-roll any of these (GAL-02, GAL-03).

(4) Add a `<style is:global>` block (after the existing scoped `<style>`) overriding PhotoSwipe variables to the dark/pink palette: `.pswp { --pswp-bg: #000; --pswp-icon-color: #f2f0ee; --pswp-icon-color-secondary: #9a9691; --pswp-error-text-color: #f3a9bd; }`, hide the raw caption source with `.hidden-caption-content { display: none; }`, and style `.pswp__custom-caption { font-family: var(--font-sans); font-size: 13px; color: var(--color-text-muted); text-align: center; padding: 8px 16px; }`. Update the component's JSDoc header to note the lightbox is now wired (GAL-01..03).
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"gallery-grid\"" src/components/GallerySection.astro` returns `1`.
    - Grid items are anchors with PhotoSwipe data attributes: `grep -c "data-pswp-width" src/components/GallerySection.astro` ≥ 1 and `grep -c "data-pswp-height" src/components/GallerySection.astro` ≥ 1.
    - PhotoSwipe imported in a NON-inline script: `grep -c "from 'photoswipe/lightbox'" src/components/GallerySection.astro` returns `1` AND `grep -n "script is:inline" src/components/GallerySection.astro` returns nothing (Pitfall 1).
    - CSS import present: `grep -c "import 'photoswipe/style.css'" src/components/GallerySection.astro` returns `1`.
    - Dark theming present: `grep -c "\\-\\-pswp-bg: #000" src/components/GallerySection.astro` ≥ 1.
    - Caption wiring present: `grep -c "hidden-caption-content" src/components/GallerySection.astro` ≥ 2 (the span + the JS lookup + the CSS rule).
    - `npx astro build` exits 0 and the dist output bundles photoswipe CSS (the build does not silently drop the style import — verify a .css asset referencing pswp exists under dist/_astro, e.g. `find dist -name '*.css' | xargs grep -l 'pswp' | head -1` returns a file).
  </acceptance_criteria>
  <done>Gallery grid is anchor-wrapped with full-res dimensions; PhotoSwipe 5 initialized via a bundled script with dark/pink theming and optional caption support; production build bundles the CSS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add real captions to demonstrate D-18 optional-caption behavior</name>
  <read_first>
    - src/content/gallery/01-studio.json (and the other 4 gallery JSONs — current shape: filename, alt, gridSpan, order)
    - src/content.config.ts (confirm caption field exists from 02-01)
    - .planning/phases/02-media-islands/02-UI-SPEC.md (caption used sparingly for photographer credit / location)
  </read_first>
  <action>
Add a `caption` field to exactly 2 of the 5 gallery JSON files (leave the other 3 without one, to prove the "only when present" behavior). Suggested: add `"caption"` to `01-studio.json` (e.g. a studio/location credit) and `05-live.json` (e.g. a live-show location/credit) using short Russian text appropriate to photographer-credit or location framing. Do not add captions to the remaining 3 — they must render counter-only in the lightbox. Keep all existing fields unchanged.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 gallery JSON files contain a non-empty `caption`: `grep -l '"caption"' src/content/gallery/*.json | wc -l` returns `2`.
    - The other 3 gallery JSON files have no caption key: `grep -L '"caption"' src/content/gallery/*.json | wc -l` returns `3`.
    - `npx astro check` reports 0 errors (caption is optional in the schema; absence validates fine).
  </acceptance_criteria>
  <done>Two gallery photos carry a real caption; three carry none — D-18 optional behavior is demonstrable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify lightbox open/navigate/close + caption on desktop and mobile</name>
  <what-built>
The gallery grid now opens a fullscreen dark lightbox on click, with arrow/keyboard/swipe navigation, an "N / M" counter, optional captions, and three close methods. Run `npm run dev` (or the Vercel preview) and test the gallery section.
  </what-built>
  <how-to-verify>
Desktop (browser):
1. Click any gallery photo → it opens fullscreen over a black background. (GAL-01)
2. Use the on-screen arrow buttons AND the keyboard ← / → to move between photos. (GAL-02)
3. Confirm a counter like "3 / 5" is visible. (GAL-02)
4. Open a photo that HAS a caption (01-studio / 05-live) → the caption shows below the image; open one without → no caption, just the counter (clean). (D-18)
5. Close three ways: click × , press Escape, and click outside the image. Each closes the lightbox. (GAL-03)
6. Confirm the lightbox chrome is dark with the project palette (not PhotoSwipe's default light blue) — this proves the CSS bundled correctly (Pitfall 1 regression check).

Mobile (real phone or device emulation):
7. Tap a photo → opens fullscreen; swipe left/right to navigate; the counter updates; tap outside or the × closes it. (GAL-02, GAL-03)
  </how-to-verify>
  <resume-signal>Confirm checks 1-7 passed (especially #6 — dark theming proves CSS bundled, the known Vercel/Astro pitfall). Type "approved" or describe any failure (e.g. lightbox opens in a new tab / no styles → is:inline regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gallery image assets | Images are Astro-optimized local assets served same-origin over HTTPS; no external fetch, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | photoswipe npm package (client-side bundle) | accept | `photoswipe` is MIT, ~11yrs old, [OK] in RESEARCH Package Legitimacy Audit + UI-SPEC Registry Safety (no eval, no network, no process.env). Already in package.json. |
| T-02-07 | Information Disclosure | Caption text could leak unintended info | accept | Captions are author-curated short credit/location strings in repo-controlled JSON; no PII, no user-supplied content. |

No serverless, auth, secrets, or external network calls in this plan. Lightbox is pure client-side over same-origin assets.
</threat_model>

<verification>
- `npx astro build` exits 0; PhotoSwipe CSS is present in the dist bundle (Pitfall 1 guard).
- Lightbox opens fullscreen, navigates (arrows/keyboard/swipe), shows counter, closes 3 ways.
- Captioned photos show a caption; uncaptioned photos show counter only (D-18).
- Lightbox chrome uses the dark/pink palette.
</verification>

<success_criteria>
- GAL-01: clicking a photo opens it fullscreen in a lightbox.
- GAL-02: navigate via arrows, keyboard, and swipe; position counter shown.
- GAL-03: close via ×, click-outside, and Escape.
</success_criteria>

<output>
Create `.planning/phases/02-media-islands/02-03-SUMMARY.md` when done. Note whether PhotoSwipe CSS bundled correctly on the Vercel preview (Pitfall 1 outcome).
</output>
