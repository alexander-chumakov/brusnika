---
phase: 01-foundation-static-site
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Nav.astro
  - src/components/MarqueeSection.astro
  - src/components/FeaturedAlbumSection.astro
  - src/components/AboutSection.astro
  - src/components/SingSection.astro
  - src/pages/index.astro
autonomous: false
requirements: [FND-01, FND-04]
must_haves:
  truths:
    - "Visitor sees a fixed blurred top nav (logo dot + «внимание брусника!» + menu links + «пойте с нами» pill)"
    - "Visitor sees the scrolling Prata marquee strip between hero and album"
    - "Visitor sees the Неоднозначное featured-album banner with its «слушать целиком» CTA"
    - "Visitor sees the «о группе» about section (text + portrait) and the «пойте с нами» section (image + хоротерапия/индивидуальные blocks + «записаться» button)"
    - "All these sections collapse correctly on mobile (nav menu hidden, grids become single-column) matching the draft @media rules"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "fixed top navigation"
      min_lines: 15
    - path: "src/components/MarqueeSection.astro"
      provides: "scrolling marquee strip"
    - path: "src/components/FeaturedAlbumSection.astro"
      provides: "Неоднозначное album banner with optimized <Image>"
    - path: "src/components/AboutSection.astro"
      provides: "о группе section with portrait <Image>"
    - path: "src/components/SingSection.astro"
      provides: "пойте с нами section with image + lesson blocks + записаться button"
  key_links:
    - from: "src/pages/index.astro"
      to: "the five new section components"
      via: "import + composition in document order"
      pattern: "FeaturedAlbumSection"
---

<objective>
Add the static prose sections that need no data layer: the fixed Nav, the Marquee strip, the Featured Album banner, the About section, and the Sing-with-us section. Each is a pixel-faithful port from the draft, composed into `index.astro` in document order, leaving the deployed preview strictly more complete than after the skeleton.

Purpose: Build out the bulk of the page's visible surface using the proven Layout + token + Image pipeline, before introducing Content Collections (Plan 03) and links-as-data (Plan 04).
Output: A Vercel preview showing hero → nav → marquee → featured album → about → sing-with-us, all matching the draft on mobile and desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-static-site/01-CONTEXT.md
@.planning/phases/01-foundation-static-site/01-01-SUMMARY.md

<!-- Pixel source of truth. NAV = index.html lines 64-76; MARQUEE = 106-112; FEATURED ALBUM = 114-129; ABOUT = 176-189; SING = 243-275. -->
@index.html

<interfaces>
<!-- Tokens + global classes established in Plan 01. Reuse, do not redefine. -->
From src/styles/global.css: --color-bg, --color-bg-raised (#070707, used by About), --color-accent (#f3a9bd),
  --color-text, --color-text-muted, --color-text-dim, --font-serif, --font-sans, --font-mono;
  global interactive classes: .nav-link, .btn-pink; scroll-reveal attrs: data-reveal, data-reveal-d1.
Real outbound URLs already in the draft (keep as-is this plan): Spotify https://open.spotify.com/artist/11epkfgLUMWmXsTbgfPu4B ;
  хоротерапия Timepad https://sonya-brusnika.timepad.ru/event/4010316/ .
Hook for Plan 04: the nav «пойте с нами» pill AND the SingSection «записаться» button carry the attribute `data-open-lessons`
  (the lessons-modal open hook). Render the attribute now; the JS that reads it lands in Plan 04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav, Marquee, and Featured Album sections</name>
  <files>src/components/Nav.astro, src/components/MarqueeSection.astro, src/components/FeaturedAlbumSection.astro</files>
  <read_first>
    - index.html lines 64-76 (NAV: logo dot + Prata wordmark, .nav-link menu «релизы»/«о группе»/«концерты», pink «пойте с нами» pill with data-open-lessons)
    - index.html lines 106-112 (MARQUEE: two duplicated Prata text-stroke spans, animation:marquee 24s linear infinite)
    - index.html lines 114-129 (FEATURED ALBUM: section id="music", banner image featured.jpg, left gradient, eyebrow «дебютный альбом · 2024», Prata h2 «Неоднозначное», description, «слушать целиком» Spotify CTA + «11 треков · 38 мин»)
    - .planning/phases/01-foundation-static-site/01-RESEARCH.md § "Pattern 2" (<Image>), § "Pitfall 6" (inline whitespace)
    - src/layouts/Layout.astro (to know where the slot and body wrapper are) and src/styles/global.css (token + class names)
  </read_first>
  <action>
    Create `src/components/Nav.astro`: port the fixed `<nav>` (lines 65-76) exactly — backdrop-blur(16px), translucent black bg, bottom border `var(--color-border-subtle)`, logo (9px pink dot with glow + Prata wordmark «внимание брусника!»), `.nav-menu` with three `.nav-link` anchors (`#music` «релизы», `#about` «о группе», `#shows» «концерты») and the pink «пойте с нами» pill. The pill MUST carry `data-open-lessons` and `href="#sing"`. Move layout CSS to a scoped `<style>`; keep `.nav-menu` and the `@media (max-width:860px)` rule that hides it on mobile (FND-04). Reference color tokens.

    Create `src/components/MarqueeSection.astro`: port the marquee strip (lines 107-112) — overflow-hidden bordered band, `background:#040404`, one `display:inline-flex; animation:marquee 24s linear infinite` containing the two identical Prata `text-stroke` spans «внимание брусника! ✶ dream-pop ✶ … ✶ live ✶». Use `var(--font-serif)` and `var(--color-accent)` for the stroke. Scoped layout styles.

    Create `src/components/FeaturedAlbumSection.astro`: port `<section id="music">` (lines 115-129). Use Astro `<Image>` for the banner: `import featuredImg from '../assets/images/featured.jpg'`, `widths={[768,1180]}`, `sizes="100vw"`, `format="webp"`, `quality={85}`, `loading="lazy"`, preserving `aspect-ratio:16/9;object-fit:cover`. Keep the left gradient overlay, the `data-reveal` wrapper, eyebrow, Prata h2 «Неоднозначное», description, and the «слушать целиком» anchor to the real Spotify URL with `target="_blank" rel="noopener noreferrer"`, plus the «11 треков · 38 мин» span. Scoped layout styles referencing tokens.
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npm run build 2>&1 | tail -3 && grep -q 'data-open-lessons' src/components/Nav.astro && grep -q 'animation:marquee\|animation: marquee' src/components/MarqueeSection.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `Nav.astro` contains the `data-open-lessons` attribute on the «пойте с нами» pill and a `@media (max-width:860px)` rule hiding `.nav-menu`
    - `MarqueeSection.astro` uses `animation:marquee ... 24s linear infinite` and `var(--font-serif)`
    - `FeaturedAlbumSection.astro` renders an optimized `<Image>` (webp derivative in `dist/`) and the Spotify CTA uses `rel="noopener noreferrer"`
  </acceptance_criteria>
  <done>Nav, marquee, and featured-album sections built pixel-faithfully with optimized featured image.</done>
</task>

<task type="auto">
  <name>Task 2: About + Sing-with-us sections, compose all into index.astro</name>
  <files>src/components/AboutSection.astro, src/components/SingSection.astro, src/pages/index.astro</files>
  <read_first>
    - index.html lines 176-189 (ABOUT: section id="about", bg #070707, .about-grid two columns, eyebrow «о группе», Prata h2 «Мы собираем хрупкое в песни», two paragraphs, about.jpg portrait in 3/4 frame)
    - index.html lines 243-275 (SING: section id="sing", bg #050505, .sing-grid, live-carpet.jpg image 4/5, eyebrow «пойте с нами», Prata h2 «Голос есть у каждого», intro paragraph, two numbered blocks Хоротерапия / Индивидуальные уроки вокала, «записаться →» btn-pink with data-open-lessons)
    - index.html lines 43-54 (the @media block: .about-grid and .sing-grid become 1fr on mobile)
    - src/pages/index.astro (current composition — hero only) and src/components/Nav.astro, MarqueeSection.astro, FeaturedAlbumSection.astro from Task 1
  </read_first>
  <action>
    Create `src/components/AboutSection.astro`: port `<section id="about">` (lines 177-189). Background `var(--color-bg-raised)` (#070707). `.about-grid` two-column (1.15fr 0.85fr) with `data-reveal` on the text column and `data-reveal data-reveal-d1` on the image column. Eyebrow «о группе», Prata h2 «Мы собираем<br>хрупкое <span style="color:var(--color-accent)">в песни</span>», the two body paragraphs (text muted), and the portrait via `<Image src={aboutImg}>` (`import aboutImg from '../assets/images/about.jpg'`, `widths={[480,860]}`, `format="webp"`, `loading="lazy"`) inside the `aspect-ratio:3/4` frame. Include the scoped `.about-grid` style AND the mobile single-column rule (FND-04).

    Create `src/components/SingSection.astro`: port `<section id="sing">` (lines 244-275). Background `#050505`. `.sing-grid` (0.92fr 1.08fr). Left: `<Image src={singImg}>` (`live-carpet.jpg`, `format="webp"`, `loading="lazy"`, `object-position:center 30%`) in the 4/5 frame with the bottom gradient. Right: eyebrow, Prata h2 «Голос есть у каждого», intro paragraph, the two numbered blocks (01 Хоротерапия, 02 Индивидуальные уроки вокала) with their copy, and the `<button class="btn-pink" data-open-lessons>записаться →</button>`. Include the mobile single-column rule. Reference tokens.

    Update `src/pages/index.astro`: import the five new components plus the existing HeroSection, and compose them inside `<Layout>` in exact document order: `Nav`, `HeroSection`, `MarqueeSection`, `FeaturedAlbumSection`, `AboutSection`, `SingSection`. (Tracks/Shows/Gallery/Footer slots come in later plans — leave a clear ordering comment marking where Plan 03 inserts Tracks/Shows/Gallery between FeaturedAlbum and About, and where Plan 04 inserts Footer + modal markup at the end, matching the draft order: hero → marquee → featured → tracks → about → shows → gallery → sing → footer.)
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npm run build 2>&1 | tail -3 && grep -q 'data-open-lessons' src/components/SingSection.astro && grep -q 'SingSection\|AboutSection' src/pages/index.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `AboutSection.astro` and `SingSection.astro` each contain a `@media (max-width:860px)` single-column rule (FND-04)
    - `SingSection.astro` «записаться» button carries `data-open-lessons`
    - `index.astro` composes Nav, Hero, Marquee, FeaturedAlbum, About, Sing in document order with a placement comment for the Plan 03 / Plan 04 sections
    - `dist/` contains webp derivatives for about.jpg and live-carpet.jpg
  </acceptance_criteria>
  <done>About and Sing sections built; all five static sections composed into the page in the correct draft order.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the static sections on Vercel preview</name>
  <what-built>Five static sections (nav, marquee, featured album, about, sing-with-us) added to the page and deployed to a Vercel preview, composed in the draft's document order on top of the hero.</what-built>
  <action>Human verification only — no code changes. Deploy the latest build to a Vercel preview, then walk the steps in <how-to-verify> below and compare against index.html. Resolve any mismatch before approving.</action>
  <how-to-verify>
    1. Open the new Vercel preview URL on desktop. Scroll from top: hero → fixed nav stays pinned and blurred → marquee scrolls → «Неоднозначное» album banner → (gap where tracks will go) → «о группе» on the raised dark panel → (gap for shows/gallery) → «пойте с нами» with the two lesson blocks and «записаться» button. Compare each against the matching section in `index.html` — colors, Prata headings, spacing should match.
    2. Resize to mobile width (or open on a phone): confirm the nav menu links hide, and the about + sing grids stack to a single column.
    3. Hover the nav links and the pink buttons — confirm the hover color shifts match the draft.
    4. The «пойте с нами» pill and «записаться» button will not open a modal yet (that JS lands in Plan 04) — that is expected.
  </how-to-verify>
  <resume-signal>Type "approved" if all five sections match the draft on desktop and mobile, or describe any visual mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → Vercel CDN | Static prerendered markup only; no input accepted |
| Page → external destinations | Outbound links to Spotify and Timepad open in new tabs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Information Disclosure | `target="_blank"` outbound links (Spotify CTA, хоротерапия link in Sing copy) | mitigate | All `target="_blank"` anchors include `rel="noopener noreferrer"` to prevent reverse-tabnabbing and referrer leakage (ASVS L1) |
| T-02-02 | Spoofing | Marquee/featured are pure static text; no dynamic content | accept | No untrusted data is rendered; nothing to spoof |
</threat_model>

<verification>
- `npm run build` exits 0; `astro check` passes
- All five sections render and compose into index.astro in draft order
- Mobile @media rules collapse about/sing grids and hide nav menu
- All `target="_blank"` links use `rel="noopener noreferrer"`
- Vercel preview visually faithful to the corresponding draft sections
</verification>

<success_criteria>
- Nav, marquee, featured-album, about, sing sections built pixel-faithfully (FND-01)
- Responsive behavior matches the draft on mobile and desktop (FND-04)
- Composed into index.astro in the draft's document order
- Modal-open hooks (`data-open-lessons`) present for Plan 04 to wire
</success_criteria>

<output>
Create `.planning/phases/01-foundation-static-site/01-02-SUMMARY.md` when done. Record: the exact insertion-point comments left in index.astro for Plans 03/04, any token additions to global.css, and any whitespace-stripping fixes needed (RESEARCH Pitfall 6 / open question 2).
</output>
