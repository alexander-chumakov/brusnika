---
quick_id: 260626-ld1
slug: full-discography-year-filter
type: quick
autonomous: true
created: 2026-06-26
---

<objective>
Extend the releases hero slideshow (ReleasesSection.astro) to hold the band's FULL real discography (2021–2026, 18 releases) and make the year selector a STRICT FILTER: selecting a year shows ONLY that year's releases as banner slides; the prev/next arrows and progress dots cycle WITHIN the selected year only (never across years). Releases that have a photo show the photo; the rest show a themed striped placeholder tile. Keep the canonical big-banner look, the section id="music", and the singles list (TracksSection) below — unchanged.
</objective>

<current_state>
- src/data/releases.ts currently exports `ReleaseSlide[]` with 3 items (Доодури/Преисполненный/Неоднозначное). REPLACE with the full model + data below.
- src/components/ReleasesSection.astro is the current single-track hero slideshow (3 slides, year label per slide). REWORK so the year selector filters and each year has its own slide set.
- Placeholder tile theme tokens ALREADY EXIST in src/styles/global.css: --rel-a / --rel-b / --rel-fg (night at ~line 87, day at ~line 132). Reuse them; do NOT redefine.
- src/assets/images has only 3 release photos: band-table.jpg (Доодури), g-guitar.jpg (Преисполненный), featured.jpg (Неоднозначное). The other 15 releases have NO photo yet → placeholder tile.
</current_state>

<tasks>

<task n="1" name="Rewrite releases.ts as full discography">
<files>src/data/releases.ts</files>
Export `interface Release { year:string; type:string; title:string; eyebrow:string; meta:string; desc?:string; image?:ImageMetadata; imagePosition?:string; url:string }` and `const releases: Release[]`.
- `image` is OPTIONAL (only 3 have it). Import the 3 images via ESM as today.
- All `url` = the Spotify artist page const (keep SP const).
- `eyebrow` = a short uppercase-style label shown above the title (e.g. "новый сингл · 2026"); for non-hero items default it to `"{type} · {year}"`.
- `desc` only for the 3 photo releases (keep existing copy); omit for the rest.
- Order newest→oldest within the array (doesn't matter for grouping but keep tidy).

DATA (18 releases — transcribe exactly; ★ = has photo):
2026:
- ★ Доодури — type "сингл", eyebrow "новый сингл · 2026", meta "сингл · 15 мая 2026", desc "Самый свежий релиз — тёплый и негромкий, как поздняя весна.", image band-table.jpg, imagePosition "center 32%"
- Весеннее танго — type "сингл", eyebrow "сингл · 2026", meta "кавер на Анну Герман"
- Home Session Live — type "live", eyebrow "live · 2026", meta "акустический live · апрель 2026"
2025:
- ★ Преисполненный — type "EP + Deluxe", eyebrow "EP + Deluxe · 2025", meta "EP · 24 октября 2025", desc "Песни о свободе и взрослении — с расширенным Deluxe-изданием.", image g-guitar.jpg, imagePosition "center 42%"
- Live Урбан 2024 — type "live", eyebrow "live EP · 2025", meta "live EP · 14 марта 2025"
- Научи меня быть — type "сингл", eyebrow "сингл · 2025", meta "сингл · 2025"
- Вспомни меня — type "сингл", eyebrow "сингл · 2025", meta "с YERKATT · 2025"
- Клуб Неоправданных Надежд — type "сингл", eyebrow "сингл · 2025", meta "сингл · 2025"
- куда летят мысли? — type "remix", eyebrow "remix · 2025", meta "remix · 2025"
2024:
- ★ Неоднозначное — type "альбом", eyebrow "дебютный альбом · 2024", meta "альбом · 25 октября 2024", desc "Одиннадцать песен о любви, памяти и взрослении. Тёплые гитары, синтезаторы и шорох плёнки.", image featured.jpg, imagePosition "center 42%"
- неактуальное — type "EP", eyebrow "EP · 2024", meta "EP · 29 января 2024"
- Безнаказанным — type "сингл", eyebrow "сингл · 2024", meta "сингл · 2024"
2023:
- Развлечение — type "сингл", eyebrow "сингл · 2023", meta "сингл · 2023"
- Пари — type "сингл", eyebrow "сингл · 2023", meta "сингл · 2023"
2022:
- Занавес — type "сингл", eyebrow "сингл · 2022", meta "сингл · 2022"
- Осколки — type "сингл", eyebrow "сингл · 2022", meta "сингл · 2022"
- Божья Коровка — type "сингл", eyebrow "сингл · 2022", meta "сингл · 2022"
2021:
- босиком — type "EP", eyebrow "EP · 2021", meta "EP · 2021"
- как поймать себя (?) — type "мини-альбом", eyebrow "мини-альбом · 2021", meta "мини-альбом · 2021"
</task>

<task n="2" name="Rework ReleasesSection.astro for year-filtered slideshow">
<files>src/components/ReleasesSection.astro</files>
Keep <section id="music">, the «релизы» eyebrow, the Prata-italic year selector with ▾ caret + dropdown, the big banner, progress bars, prev/next arrows — same visual language as now. Changes:

DATA GROUPING (in frontmatter): import `releases`, derive `years = [...new Set(releases.map(r=>r.year))].sort().reverse()` → ["2026","2025","2024","2023","2022","2021"]. Group: `byYear = years.map(y => ({ year:y, items: releases.filter(r=>r.year===y) }))`. Default active year = years[0] ("2026").

MARKUP: render ONE banner that contains, per year, a "year panel" (a flex slide-track of that year's items). Wrap each year-panel in a container with `data-year="{year}"`. Only the active year's panel is shown (others display:none). Each item renders as a full-bleed slide (flex:0 0 100%):
  - If item.image: astro:assets <Image> + left→right dark gradient + WHITE content (eyebrow #f3a9bd, Prata white title, optional desc #d8d4cf, pink CTA + meta #cfcbc6) — exactly like the current photo slides.
  - Else (no image): a PLACEHOLDER tile filling the slide — `background: repeating-linear-gradient(135deg, var(--rel-a) 0 15px, var(--rel-b) 15px 30px)` + a soft pink radial; content block uses THEME-readable colors (eyebrow var(--accent); title Prata var(--rel-fg) or var(--text); meta var(--text-dim); pink CTA same as photo slides) so it reads in BOTH day and night. A small pink dot top-left as a brand accent.
The year selector dropdown lists ALL years in `years` (not a fixed 3). Active year highlighted accent+bold.
Progress bars + arrows belong to the banner but operate on the ACTIVE year's item count (N segments = active year's items).

INLINE <script> (vanilla, null-safe, scoped to the section) — STRICT year filter:
- State: `activeYearIndex` (into years[]), and `slide` index within the active year's items.
- Show only the active year's panel (toggle display); set the year-button label to the active year; update dropdown active styling + caret rotation.
- Build/refresh the progress dots to match the active year's item count; translate the active panel's track by `-slide*100%`.
- prev/next arrows: change `slide` within the active year ONLY, wrap modulo that year's item count. They MUST NOT move to another year.
- Year dropdown buttons: set activeYear to the picked year, reset slide=0, refresh panel/dots, close menu.
- Autoplay: setInterval 6000ms advancing `slide` within the active year (wrap). Pause on banner hover; restart on manual nav (arrow or year pick) and on year change; reset the fillbar animation on each slide change. If the active year has only 1 item, no autoplay and hide/zero the dots gracefully. Respect prefers-reduced-motion (no autoplay, no fill animation; arrows still work).
- Outside-click overlay closes the year dropdown.
Keep the `@keyframes fillbar` and scoped styles; add styles for the placeholder tile. Ensure a good mobile treatment.
</task>

<task n="3" name="Build verify">
<files>src/pages/index.astro</files>
Confirm index.astro still renders <ReleasesSection /> then <TracksSection /> (no change expected). Run `npm run build`; it MUST compile with zero errors. NO Google Fonts. Fix any error before finishing.
</task>

</tasks>

<constraints>
- Year selector is a STRICT filter — arrows never cross years (the user's explicit requirement).
- Keep id="music"; do not touch TracksSection.astro / AudioPlayer / content collections.
- Reuse existing --rel-* tokens (already in global.css); do not redefine theme tokens.
- Both day and night must look right: photo slides = white-on-photo; placeholder slides = readable theme colors on the striped tile.
- All CTAs → Spotify artist page (per-release links deferred; note in SUMMARY).
</constraints>

<success_criteria>
- releases.ts holds all 18 real releases grouped-able by 6 years; only 3 carry images.
- Selecting 2026 shows only 2026's 3 releases; 2025 only its 6; 2024 only 3; 2023 only 2; 2022 only 3; 2021 only 2. Arrows/dots stay within the selected year.
- Releases without photos render as themed placeholder tiles, readable in both themes.
- `npm run build` passes.
</success_criteria>

<notes>
Data assumptions to flag in SUMMARY (the user will confirm on review): the 2022/2023 split (Занавес/Осколки/Божья Коровка→2022; Развлечение/Пари→2023) and several singles dated by year only.
</notes>
