---
phase: 04-test-deploy-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/robots.txt
  - .planning/phases/04-test-deploy-verification/04-VERIFICATION.md
autonomous: true
requirements: [FND-02, FND-07, AUD-05, LINK-01, LINK-02, BOOK-02, SHARE-02, SHARE-03]
user_setup: []

must_haves:
  truths:
    - "The live deploy serves /robots.txt with User-agent: * and Disallow: /"
    - "The live HTML contains <meta name=\"robots\" content=\"noindex, nofollow\">"
    - "The live HTML and its linked CSS contain zero references to fonts.googleapis.com or fonts.gstatic.com"
    - "D-02: Claude automates every curl/code-checkable criterion against the live URL (SC1 meta, SC2 fonts, SC3 server-side range, SC4 links, SC5 endpoint reachability) and records literal proof"
    - "A CDN audio URL returns Accept-Ranges: bytes and HTTP 206 + Content-Range on a ranged request"
    - "Every outbound link in the live HTML has its HTTP status recorded and every '#' placeholder is inventoried by location (D-05: inventory only, not fixed)"
    - "The /api/book endpoint is reachable and the Telegram bot token is absent from all client-side JS/HTML"
    - "D-04: A single band-shareable verification doc exists with a concrete proof per success criterion plus a placeholder-link tracker block"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawl-disallow file served at site root (D-01)"
      contains: "Disallow: /"
    - path: ".planning/phases/04-test-deploy-verification/04-VERIFICATION.md"
      provides: "Band-shareable status report covering all 5 success criteria + placeholder-link tracker + human-check instructions"
      contains: "Placeholder-link tracker"
  key_links:
    - from: ".planning/phases/04-test-deploy-verification/04-VERIFICATION.md"
      to: "https://vnimanie-brusnika.vercel.app"
      via: "recorded curl proof values (header strings, status codes) per criterion"
      pattern: "vnimanie-brusnika.vercel.app"
---

<objective>
Close the single code gap that blocks Success Criterion 1 — there is no robots.txt on the live deploy (confirmed: /robots.txt returns 404) — then run every check that can be performed from curl/code against the live private deploy and record the findings in one band-shareable document.

Purpose: Give the band a single status report proving v1 is private (noindex + robots.txt), uses self-hosted fonts (no Google Fonts), serves seekable audio, has every outbound link audited, and has a reachable booking endpoint — with concrete proof values, not vague "looks fine".
Output: `public/robots.txt` (deployed live) and `04-VERIFICATION.md` (all automated criteria filled with proof; the two human checks left as ⏳ pending with plain-language Russian instructions for Plan 02).

Decision coverage in this plan: D-01 (create the static disallow-all robots.txt), D-02 (Claude automates the curl/code SC1–SC5 server-side checks), D-04 (record findings in one band-shareable markdown doc), D-05 (placeholder links inventoried only, not fixed). The two human checks (D-03) and the real live-booking test (D-06) are handled in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-deploy-verification/04-CONTEXT.md

# Verification targets — read to know exactly what the live deploy should contain:
@src/layouts/Layout.astro
@src/data/site.ts
@src/pages/api/book.ts
@astro.config.mjs

<facts>
- Live deploy URL (already noindex): https://vnimanie-brusnika.vercel.app
- /robots.txt currently returns 404 — this is the one gap to fix (D-01).
- The noindex meta tag is build-time gated on import.meta.env.NOINDEX === 'true' (Layout.astro line 33,49) and is already present on the live deploy (NOINDEX env var set in Vercel).
- A representative CDN audio URL: https://pacvdizqnhsygnis.public.blob.vercel-storage.com/audio/doorudi.mp3
- Vercel CLI is linked (.vercel/project.json exists), so `vercel --prod` can deploy the new file.
- '#' placeholder outbound links live in two places:
  - src/data/site.ts: Яндекс Музыка, VK Музыка, Apple Music (streamingLinks); VK community, Instagram (socialLinks).
  - src/content/shows/*.json: all 4 shows (Москва, СПб, Казань, Екатеринбург) omit ticketUrl, so it defaults to '#' (content.config.ts) and renders as href="#".
- Real outbound links present: Spotify, YouTube, Telegram (t.me/vnimaniebrusnika), Timepad horoterapiya, mailto:hello@brusnika.ru.
- /api/book honeypot path: a POST with the `website` field filled returns 200 {"ok":true} WITHOUT contacting Telegram (book.ts line 92-98) — use this as a safe reachability probe that does NOT spam Соня.
- The Telegram bot token is read only from process.env (no PUBLIC_ prefix), so it must never appear in any client bundle.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/robots.txt and deploy it to the live site</name>
  <files>public/robots.txt</files>
  <read_first>
    - public/ (currently holds only favicon.svg — confirms this is the static-asset root served verbatim at site root)
    - astro.config.mjs (output: 'static', @astrojs/vercel with staticHeaders: true — files in public/ ship as-is)
    - .planning/phases/04-test-deploy-verification/04-CONTEXT.md § D-01
  </read_first>
  <action>
    D-01: Create `public/robots.txt` containing exactly two directive lines: a `User-agent: *` line followed by a `Disallow: /` line (crawl-disallow everything, always; do NOT env-gate it, v1 stays private throughout). No other directives.

    Then deploy the change to the live production deploy so the file is served at /robots.txt. Use the project's linked Vercel CLI: run `vercel --prod` from the repo root (the project is linked via .vercel/project.json). If the deploy requires authentication Claude does not have, stop and surface it as an auth gate rather than guessing. After the deploy reports complete, poll the live URL until it serves the file (Vercel production deploys typically finish in under ~90s): `curl -s https://vnimanie-brusnika.vercel.app/robots.txt`.

    Do NOT modify any other source file. This is the only code change in the phase.
  </action>
  <verify>
    <automated>curl -s https://vnimanie-brusnika.vercel.app/robots.txt | grep -q 'Disallow: /' && curl -s -o /dev/null -w '%{http_code}' https://vnimanie-brusnika.vercel.app/robots.txt | grep -q '200'</automated>
  </verify>
  <acceptance_criteria>
    - `public/robots.txt` exists and contains the line `User-agent: *` and the line `Disallow: /` and nothing crawl-permitting.
    - `curl -s https://vnimanie-brusnika.vercel.app/robots.txt` returns HTTP 200 (no longer 404) and its body contains `Disallow: /`.
    - No source file other than `public/robots.txt` was changed.
  </acceptance_criteria>
  <done>The disallow-all robots.txt is live: GET /robots.txt returns 200 with Disallow: /.</done>
</task>

<task type="auto">
  <name>Task 2: Run all automated verification checks against the live deploy</name>
  <files>(none — read-only audit; results are recorded into 04-VERIFICATION.md in Task 3)</files>
  <read_first>
    - src/layouts/Layout.astro (lines ~33, 49 — the noindex meta gate; confirms exact string `noindex, nofollow`)
    - src/data/site.ts (the authoritative outbound-link inventory and which entries are '#')
    - src/content/shows/*.json (confirm all 4 omit ticketUrl → render href="#")
    - src/pages/api/book.ts (honeypot path returns 200 without Telegram; token from process.env only)
  </read_first>
  <action>
    D-02: Claude automates every check that is verifiable from curl/code against the live URL — SC1 meta, SC2 fonts, SC3 server-side range, SC4 link audit, SC5 endpoint reachability — and captures literal proof for the doc in Task 3. Run each check below and record the exact proof value (header string, count, or status code). Do not paraphrase results — record literal values.

    SC1 (meta, server-side): fetch the homepage HTML with `curl -s https://vnimanie-brusnika.vercel.app/` and confirm it contains `name="robots"` with content `noindex, nofollow`. Also capture any X-Robots-Tag response header via `curl -sI https://vnimanie-brusnika.vercel.app/` (record if present; the meta tag + robots.txt are the gating proofs).

    SC2 (fonts): grep the homepage HTML for `fonts.googleapis.com` and `fonts.gstatic.com` — expect a count of 0. Then extract each linked CSS file href from the HTML (every `href="….css"`), fetch each stylesheet, and grep those too — expect 0 across all CSS (a Google Fonts reference could hide inside a bundled stylesheet, so the CSS grep is mandatory, not optional). Record the literal count (must be 0) and confirm @font-face URLs point to self-hosted `/_astro/` assets.

    SC3 (server-side, audio range): target the CDN audio URL `https://pacvdizqnhsygnis.public.blob.vercel-storage.com/audio/doorudi.mp3`. Run `curl -sI` on it and capture the `Accept-Ranges` header (expect `bytes`). Then force a partial response with a ranged request: `curl -s -D - -o /dev/null -H "Range: bytes=1000-2000" <url>` and capture the status line (expect `206`) and the `Content-Range` header (expect `bytes 1000-2000/<total>`). Record all three literal header/status values.

    SC4 (link audit): extract every `href="..."` from the live homepage HTML. For each real (non-`#`, non-`mailto`) destination, run `curl -sI -o /dev/null -w "%{http_code}"` and record its status code (note: some hosts may return 3xx redirects or bot-block with 403 — record the literal code honestly, do not assume 200). Cross-reference against site.ts and the shows collection so nothing is missed. Build the full list of `#` placeholders with their location (the 5 site.ts entries: Яндекс Музыка, VK Музыка, Apple Music, VK community, Instagram; plus the 4 shows ticket links).

    SC5 (server-side reachability): probe the booking endpoint WITHOUT spamming Соня by using the honeypot path — `curl -s -o /dev/null -w "%{http_code}" -X POST https://vnimanie-brusnika.vercel.app/api/book -F "website=probe" -F "name=probe"` — expect 200 (honeypot returns ok without delivering). Record the status code as proof the endpoint is deployed and reachable.

    Security/SHARE confirmation (static, from the live bundle): grep the homepage HTML and every linked JS bundle for `api.telegram.org`, `TELEGRAM_BOT_TOKEN`, and any bot-token-shaped string — expect 0 occurrences (token is server-side only). Also confirm the share UI is present in the live HTML (the clip share button/markup) and that the desktop fallback share destinations to t.me and vk.com appear (SHARE-02/03 — these are part of the SC4 link set). Record the literal token-grep count (must be 0).
  </action>
  <verify>
    <automated>bash -c 'set -e; BASE=https://vnimanie-brusnika.vercel.app; H=$(curl -s "$BASE/"); echo "$H" | grep -q "noindex, nofollow"; test $(echo "$H" | grep -c "fonts.googleapis.com\|fonts.gstatic.com") -eq 0; for css in $(echo "$H" | grep -oE "href=\"[^\"]+\.css\"" | sed -E "s/href=\"//; s/\"$//"); do case "$css" in http*) U="$css";; /*) U="$BASE$css";; *) U="$BASE/$css";; esac; test $(curl -s "$U" | grep -c "fonts.googleapis.com\|fonts.gstatic.com") -eq 0; done; curl -sI "https://pacvdizqnhsygnis.public.blob.vercel-storage.com/audio/doorudi.mp3" | grep -iq "accept-ranges: bytes"; curl -s -D - -o /dev/null -H "Range: bytes=1000-2000" "https://pacvdizqnhsygnis.public.blob.vercel-storage.com/audio/doorudi.mp3" | grep -q "206"; test $(echo "$H" | grep -c "api.telegram.org") -eq 0; test $(curl -s -o /dev/null -w "%{http_code}" -X POST "$BASE/api/book" -F "website=probe" -F "name=probe") -eq 200'</automated>
  </verify>
  <acceptance_criteria>
    - (D-02) Live HTML contains the exact string `noindex, nofollow` inside a `name="robots"` meta tag.
    - `fonts.googleapis.com` and `fonts.gstatic.com` appear 0 times in the live HTML AND in every linked CSS file (the automated gate fetches and greps each linked stylesheet, so a reference hidden in a CSS bundle fails the gate).
    - The CDN audio URL response includes `Accept-Ranges: bytes`; a `Range: bytes=1000-2000` request returns HTTP `206` with a `Content-Range: bytes 1000-2000/<total>` header. All three literal values captured.
    - Every outbound link's literal HTTP status code is recorded; the complete `#`-placeholder list (5 site.ts + 4 show tickets = 9) is compiled with each one's location.
    - POST to /api/book with the honeypot field returns HTTP 200 (asserted in the automated gate), proving reachability, with no message sent to Соня.
    - `api.telegram.org` / `TELEGRAM_BOT_TOKEN` / any token string appears 0 times in the live HTML and JS bundles.
  </acceptance_criteria>
  <done>All five success criteria's automatable halves have literal proof values captured, plus the token-absence and link-status records, ready to write into the doc.</done>
</task>

<task type="auto">
  <name>Task 3: Write the band-shareable verification document</name>
  <files>.planning/phases/04-test-deploy-verification/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-test-deploy-verification/04-CONTEXT.md § D-04 (doc structure), § D-05 (placeholder tracker is a handoff artifact), § specifics (band-shareable tone, plain-language Russian human instructions)
    - src/data/site.ts (placeholder locations + which real URL is pending from the band per inline comments)
  </read_first>
  <action>
    D-04: Create `.planning/phases/04-test-deploy-verification/04-VERIFICATION.md` — the one band-shareable markdown status report (inside the phase dir) covering all 5 success criteria with a proof table plus a placeholder-link tracker. It must contain, in order:

    1. A short intro line stating this is the v1 private test-deploy verification for https://vnimanie-brusnika.vercel.app (noindex; never goes public in v1).
    2. A summary table with one row per success criterion: Criterion | Requirement(s) | Status | Proof. Status uses ✅ (proven with concrete proof), ⏳ (pending human), or ⛔ (blocked) — never vague. Fill SC1, SC2, the server-side half of SC3, SC4, and the server-side half of SC5 as ✅ using the literal proof values captured in Task 2 (e.g. the actual `Accept-Ranges: bytes` string, the `206` + `Content-Range` value, the 0-count for Google Fonts, the per-link status codes summary, the 200 from the honeypot probe). Mark SC3-device and SC5-receipt as ⏳ pending human (resolved in Plan 02).
    3. D-05: a dedicated "Placeholder-link tracker" block — inventory only, do NOT fix the links. A table listing every remaining `#` link — Label | Where it lives (file) | Real URL still pending from band. Include all 9 (Яндекс Музыка, VK Музыка, Apple Music in site.ts; VK community, Instagram in site.ts; the 4 show ticket links in src/content/shows/). State clearly this is a Launch-milestone handoff list, NOT a v1 test gate.
    4. A "Что проверить на телефоне" section with two plain-language Russian, step-by-step instruction blocks for the human checks in Plan 02:
       - Audio seek (SC3 device): открыть сайт на айфоне в Safari, начать любой трек, перемотать ползунок на середину — должно играть с середины, а не сначала.
       - Booking receipt (SC5): заполнить и отправить форму записи на сайте, затем подтвердить в Telegram, что Соне пришло сообщение, правильно оформленное, на русском.
    5. A note that no secret values (bot token, chat id) are recorded anywhere in this doc — only header values, counts, and statuses.

    Do not record any token, chat id, or other secret in the doc.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/04-test-deploy-verification/04-VERIFICATION.md; test -f "$F"; grep -q "Placeholder-link tracker" "$F"; grep -q "Accept-Ranges" "$F"; grep -q "Disallow: /" "$F"; grep -qi "айфон\|safari" "$F"; test $(grep -ci "TELEGRAM_BOT_TOKEN" "$F") -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - (D-04) `04-VERIFICATION.md` exists with a summary row for each of the 5 success criteria, each ✅ rows carrying a literal proof value (header string / count / status code), and SC3-device + SC5-receipt marked ⏳.
    - (D-05) The placeholder-link tracker lists all 9 `#` links with file location and pending-source note, labelled as a Launch-milestone handoff — inventoried, not fixed.
    - Two plain-language Russian human-check instruction blocks are present.
    - No bot token, chat id, or other secret value appears anywhere in the doc.
  </acceptance_criteria>
  <done>One band-shareable doc captures every automated proof and stages the two human checks with Russian instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/book | Untrusted POST crosses into the only serverless route; touched once via a safe honeypot probe in Task 2 |
| public internet → static assets | robots.txt and HTML are public by design |
| verification doc → planning repo | The doc must not become a place secrets leak into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | /api/book Telegram token | mitigate | Task 2 greps the live HTML + JS bundles for `api.telegram.org`/`TELEGRAM_BOT_TOKEN`/token strings; must be 0 (token is process.env server-side only — book.ts line 113) |
| T-04-02 | Information Disclosure | 04-VERIFICATION.md | mitigate | Doc records only header values, counts, and status codes — never the bot token or chat id; Task 3 verify greps for token absence in the doc |
| T-04-03 | Information Disclosure | public/robots.txt | accept | Public-by-design; a disallow-all file leaks nothing sensitive — it is the intended privacy control |
| T-04-04 | Tampering / Spam | live /api/book probe | mitigate | Reachability probe uses the honeypot `website` field, which returns 200 without contacting Telegram — Соня is not spammed during verification |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — only a static file and read-only curl audits; supply-chain surface is N/A |

No `high`-severity threats exist for this phase. The surface is a static disallow-all file plus read-only audits and one honeypot-guarded endpoint probe; the only real risk (token leak) is verified-against, not introduced.
</threat_model>

<verification>
- /robots.txt live returns 200 with Disallow: / (was 404).
- Live HTML: noindex meta present; Google Fonts references = 0 in HTML and in every linked CSS bundle.
- CDN audio: Accept-Ranges: bytes + 206 + Content-Range on ranged request.
- All outbound link statuses recorded; 9 placeholder links inventoried.
- /api/book honeypot probe returns 200 (asserted in the automated gate); token absent from client bundles.
- 04-VERIFICATION.md exists, band-shareable, with placeholder tracker + Russian human instructions, no secrets.
</verification>

<success_criteria>
- public/robots.txt created (disallow-all, D-01) and confirmed live.
- Every automatable half of the 5 success criteria proven with literal values (D-02).
- Single band-shareable verification doc written (D-04); placeholders inventoried not fixed (D-05); SC3-device and SC5-receipt staged as ⏳ for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/04-test-deploy-verification/04-01-SUMMARY.md` when done.
</output>
