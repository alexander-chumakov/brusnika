---
phase: 01-foundation-static-site
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/data/site.ts
  - src/components/FooterSection.astro
  - src/components/LessonsModal.astro
  - src/components/TracksSection.astro
  - src/scripts/demo-modal.js
  - src/scripts/global-animations.js
  - src/layouts/Layout.astro
  - src/pages/index.astro
autonomous: false
requirements: [FND-01, FND-03, FND-04, LINK-01, LINK-02]
must_haves:
  truths:
    - "Visitor sees the footer with «слушать» links (Spotify, Яндекс Музыка, Apple Music, YouTube) and «связь» links (Telegram, VK, Instagram, email), sourced from data (LINK-01/02)"
    - "The tracks-header streaming links are sourced from the same site.ts data, ordered Яндекс/VK first per LINK-01"
    - "Clicking «пойте с нами» / «записаться» opens the lessons modal; submitting the form shows the fake «Спасибо!» state with NO network request (D-05)"
    - "Scroll-reveal animations fade sections in on scroll, and on a fine-pointer device the pink cursor-glow follows the mouse (FND-03)"
    - "The full page top-to-bottom matches index.html on desktop and mobile"
  artifacts:
    - path: "src/data/site.ts"
      provides: "typed singletons: streamingLinks (Яндекс/VK first), socialLinks, horoterapiyaUrl"
      contains: "streamingLinks"
    - path: "src/components/FooterSection.astro"
      provides: "footer reading streamingLinks + socialLinks from site.ts (LINK-01/02)"
    - path: "src/components/LessonsModal.astro"
      provides: "lessons modal markup (form + thanks state)"
      min_lines: 30
    - path: "src/scripts/demo-modal.js"
      provides: "isolated fake-submit modal behavior, no network call (D-05)"
    - path: "src/scripts/global-animations.js"
      provides: "scroll-reveal + cursor-glow (D-06)"
  key_links:
    - from: "src/components/FooterSection.astro"
      to: "src/data/site.ts streamingLinks + socialLinks"
      via: "import + map"
      pattern: "streamingLinks|socialLinks"
    - from: "src/scripts/global-animations.js"
      to: "[data-reveal] elements + #cursor-glow"
      via: "IntersectionObserver + mousemove (pointer:fine)"
      pattern: "IntersectionObserver"
    - from: "src/scripts/demo-modal.js"
      to: "#lessons-overlay form submit"
      via: "preventDefault then show thanks, NO fetch"
      pattern: "preventDefault"
---

<objective>
Complete the page: model outbound links as data (`site.ts`), build the footer and tracks-header links from that data (LINK-01/02), port the lessons modal with its fake submit (D-05), and ship the global animations — scroll-reveal + cursor-glow (FND-03/D-06). Finish with a full top-to-bottom pixel comparison against the draft.

Purpose: Deliver the last requirements (links-as-data, animations) and the interactive demo surfaces, then verify the whole page is visually indistinguishable from `index.html`.
Output: A complete Vercel preview of the full one-page site — every section, real animations, working demo modal, links sourced from data — matching the draft pixel-for-pixel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-static-site/01-CONTEXT.md
@.planning/phases/01-foundation-static-site/01-RESEARCH.md
@.planning/phases/01-foundation-static-site/01-03-SUMMARY.md

<!-- Pixel source of truth. FOOTER = index.html lines 277-310; LESSONS MODAL = 328-385; demo-modal JS = 427-464; animations JS = 389-410; tracks-header streaming links = 135-139. -->
@index.html

<interfaces>
<!-- Singleton data contract. From RESEARCH "src/data/site.ts — Typed Singletons" + D-02/D-03/D-07/D-08. -->
streamingLinks (ordered, Яндекс/VK first per LINK-01):
  Яндекс Музыка (url '#', D-02 user-supplied), VK Музыка (url '#', D-02 user-supplied),
  Spotify (https://open.spotify.com/artist/11epkfgLUMWmXsTbgfPu4B),
  Apple Music (url '#', D-03 Phase 4), YouTube (https://www.youtube.com/@vnimaniebrusnika).
socialLinks: Telegram (https://t.me/vnimaniebrusnika), VK (url '#', D-03), Instagram (url '#', D-03 footer-only),
  hello@brusnika.ru (mailto:hello@brusnika.ru, D-03 draft email).
horoterapiyaUrl: https://sonya-brusnika.timepad.ru/event/4010316/ (real).
NOTE: the draft footer «слушать» column lists Spotify/Яндекс/Apple/YouTube (4, no VK); the tracks-header lists
  Spotify/Яндекс/YouTube (3). LINK-01 requires Яндекс+VK first in the streamingLinks ordering — apply that ordering
  to the array; render the footer/tracks-header subsets faithfully to the draft from the same source array.
demo-modal isolation (D-05): scripts/demo-modal.js IIFE; the form submit handler does e.preventDefault() and toggles
  to the thanks state — it makes NO fetch/XHR. Phase 3 replaces ONLY this submit handler with a real /api/book call.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: site.ts singletons + Footer + tracks-header links from data (LINK-01/02)</name>
  <files>src/data/site.ts, src/components/FooterSection.astro, src/components/TracksSection.astro, src/pages/index.astro</files>
  <read_first>
    - index.html lines 277-310 (FOOTER: brand block, «слушать» column [Spotify/Яндекс Музыка/Apple Music/YouTube], «связь» column [Telegram/VK/Instagram/hello@brusnika.ru], bottom bar «© 2026» + «сделано с любовью к тихой музыке»)
    - index.html lines 135-139 (tracks-header streaming links: Spotify, Яндекс Музыка, YouTube)
    - .planning/phases/01-foundation-static-site/01-RESEARCH.md "src/data/site.ts — Typed Singletons"
    - src/components/TracksSection.astro (Plan 03 — the header links to replace with data) and src/styles/global.css (.nav-link class)
  </read_first>
  <action>
    Create `src/data/site.ts` exporting the typed singletons in this plan's `<interfaces>`: `streamingLinks` (array of `{label,url}` ordered Яндекс Музыка, VK Музыка, Spotify, Apple Music, YouTube — LINK-01 Яндекс/VK first), `socialLinks` (Telegram, VK, Instagram, mailto email), and `horoterapiyaUrl`. Use the real URLs where known (Spotify, YouTube, Telegram, Timepad) and `#`/placeholder for the rest (Яндекс/VK Музыка per D-02 user-supplied; Apple Music, VK community, Instagram per D-03 Phase 4). Mark each placeholder with an inline comment naming the deferral (D-02 or D-03) so Phase 4 can find them. Use `as const`.

    Create `src/components/FooterSection.astro`: import `streamingLinks` and `socialLinks` from `../data/site.ts`. Port `<footer>` (lines 278-310): the brand block (pink dot + Prata wordmark + descriptor paragraph). For the «слушать» column, render the draft's subset (Spotify, Яндекс Музыка, Apple Music, YouTube) by mapping the relevant `streamingLinks` entries (exclude VK Музыка here to match the draft's 4-item footer list) as `.nav-link` anchors with `target="_blank" rel="noopener noreferrer"` (skip target/_blank for `#` and mailto). For the «связь» column, map `socialLinks` similarly. Render the bottom bar exactly. Scoped styles incl. the mobile single-column `.footer-grid` rule (FND-04).

    Update `src/components/TracksSection.astro`: replace the three hardcoded header streaming anchors with a map over the relevant `streamingLinks` entries (Spotify, Яндекс Музыка, YouTube — the draft header subset), imported from `../data/site.ts`. Keep the exact draft styling (bottom-border underline, ↗). This makes LINK-01 fully data-sourced.

    Update `src/pages/index.astro`: import and append `FooterSection` as the last section (after Sing), matching draft document order.
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npm run build 2>&1 | tail -3 && grep -q 'streamingLinks' src/data/site.ts && grep -q 'streamingLinks\|socialLinks' src/components/FooterSection.astro && grep -q 'site' src/components/TracksSection.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `src/data/site.ts` exports `streamingLinks` ordered with Яндекс Музыка and VK Музыка first (LINK-01), plus `socialLinks` and `horoterapiyaUrl`
    - `FooterSection.astro` and `TracksSection.astro` both import their link lists from `site.ts` (no hardcoded link arrays)
    - Footer «слушать» and «связь» columns + bottom bar match the draft; all `target="_blank"` links use `rel="noopener noreferrer"`
    - Every placeholder `#`/draft URL carries an inline comment naming its Phase 4 / D-02 deferral
  </acceptance_criteria>
  <done>Outbound links modeled as data; footer + tracks-header render from site.ts (LINK-01/02).</done>
</task>

<task type="auto">
  <name>Task 2: Lessons modal + fake submit (D-05), global animations (FND-03/D-06), final pixel pass</name>
  <files>src/components/LessonsModal.astro, src/scripts/demo-modal.js, src/scripts/global-animations.js, src/layouts/Layout.astro</files>
  <read_first>
    - index.html lines 328-385 (LESSONS MODAL: overlay #lessons-overlay, .lessons-modal dialog #lessons-dialog, close button, left info panel with lessons.jpg bg + хоротерапия Timepad link, right #lessons-form [формат select, имя, telegram/email, опыт select, submit] and #lessons-thanks «Спасибо!» block)
    - index.html lines 427-464 (demo-modal IIFE: openLessons/closeLessons, [data-open-lessons] triggers, .lessons-close, overlay click-outside, Escape, form submit shows thanks, NO network)
    - index.html lines 389-410 (animations IIFE: IntersectionObserver scroll-reveal adding .in; cursor-glow mousemove guarded by matchMedia('(pointer:fine)'))
    - .planning/phases/01-foundation-static-site/01-RESEARCH.md "Pattern 7" (demo JS isolation, is:inline), "Pitfall 3" (reveal classes global)
    - src/layouts/Layout.astro (cursor-glow markup + where to add script tags), src/components/SingSection.astro + Nav.astro (the data-open-lessons hooks)
  </read_first>
  <action>
    Create `src/components/LessonsModal.astro`: port the full modal markup (lines 329-385) — overlay `#lessons-overlay` (display:none, fixed, blurred), `.lessons-modal #lessons-dialog` two-column dialog, `.lessons-close` button, the left info panel (the draft uses lessons.jpg as a CSS background under a gradient; since lessons.jpg now lives in `src/assets/images/`, reference it via an imported asset URL [`import lessonsImg from '../assets/images/lessons.jpg'` then use `lessonsImg.src` in an inline style], OR copy lessons.jpg into `public/` and reference `/lessons.jpg` — choose whichever keeps the gradient overlay byte-identical to the draft and record the choice in the summary), eyebrow «пойте с нами · с Соней», Prata h3 «Найдите свой голос», copy, and the two lesson blocks where Хоротерапия links to `horoterapiyaUrl` from site.ts with `rel="noopener noreferrer"`. Right panel: `#lessons-form` (формат select, имя input[required], telegram/email input[required], опыт select, «отправить заявку» submit) plus the `#lessons-thanks` «Спасибо!» block (display:none). Use the global `.field` and `.btn-pink` classes. Scoped styles incl. the mobile single-column `.lessons-modal` rule.

    Create `src/scripts/demo-modal.js`: verbatim port of the lessons-modal IIFE (lines 427-464). The submit handler MUST `e.preventDefault()` and toggle to the thanks state with NO fetch/XHR (D-05). Add a header comment: "Phase 3 replaces ONLY this submit handler with fetch('/api/book')".

    Create `src/scripts/global-animations.js`: verbatim port of the scroll-reveal + cursor-glow IIFE (lines 389-410). IntersectionObserver adds `.in` to `[data-reveal]` (threshold 0.12, unobserve after intersect); fallback adds `.in` to all if no IO. Cursor-glow `mousemove` translate guarded by `window.matchMedia('(pointer:fine)').matches` (D-06).

    Update `src/layouts/Layout.astro` (or `index.astro`, whichever matches the draft's end-of-body order — record the choice): render `<LessonsModal />` near the end of the body and add the three `<script>` references (`global-animations.js`, `demo-player.js`, `demo-modal.js`) using the directive decision recorded in Plan 03's SUMMARY (RESEARCH assumption A2). Ensure the `#cursor-glow` div from Plan 01 is present for the animation script to target.

    Final pixel pass: build with `NOINDEX=true`, run `astro check`, and walk the full page top-to-bottom against `index.html` section by section. Fix any whitespace-stripping (Pitfall 6), token, spacing, or color drift found. Re-confirm zero Google Fonts and the noindex meta after all sections are in.
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npx astro check 2>&1 | tail -3 && NOINDEX=true npm run build 2>&1 | tail -3 && grep -q 'IntersectionObserver' src/scripts/global-animations.js && grep -q 'preventDefault' src/scripts/demo-modal.js && grep -Lq 'fetch\|XMLHttpRequest' src/scripts/demo-modal.js && echo "OK demo-modal has no network call" && (grep -rq 'googleapis\|gstatic' dist/ && echo "FAIL google fonts" || echo "OK no google fonts")</automated>
  </verify>
  <acceptance_criteria>
    - `astro check` passes and `NOINDEX=true npm run build` exits 0
    - `src/scripts/demo-modal.js` contains zero `fetch`/`XMLHttpRequest` (D-05 — no network call); submit shows the thanks state
    - `src/scripts/global-animations.js` uses `IntersectionObserver` for `[data-reveal]` and guards cursor-glow with `matchMedia('(pointer:fine)')` (FND-03/D-06)
    - LessonsModal renders the form + the `#lessons-thanks` block and the хоротерапия link uses `rel="noopener noreferrer"`
    - `dist/` still contains zero `googleapis`/`gstatic` references and the noindex meta when built with NOINDEX=true
  </acceptance_criteria>
  <done>Lessons modal + fake submit, scroll-reveal + cursor-glow shipped; full page pixel-faithful to the draft.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the complete page on Vercel preview</name>
  <what-built>The complete one-page site deployed to a Vercel preview: links sourced from data, footer, the working lessons modal with fake submit, and scroll-reveal + cursor-glow animations — the full page top-to-bottom.</what-built>
  <action>Human verification only — no code changes. Deploy the latest build to a Vercel preview, then walk the steps in <how-to-verify> below and compare against index.html. Resolve any mismatch before approving.</action>
  <how-to-verify>
    1. Open the final Vercel preview URL on desktop. Scroll the entire page top-to-bottom and compare against `index.html` open side-by-side: hero → marquee → featured album → tracks → about → shows → gallery → sing → footer. Every section should be visually indistinguishable from the draft (colors, Prata/Golos fonts, spacing, gradients).
    2. As you scroll, confirm sections fade/rise into view (scroll-reveal). Move the mouse around — confirm the soft pink glow follows the cursor (desktop / fine pointer only).
    3. Click «пойте с нами» (nav pill) and «записаться» (sing section). Confirm the lessons modal opens with the two-panel layout and the lessons.jpg background. Fill in name + contact and submit — confirm it switches to the «Спасибо!» state. Open the Network tab and confirm NO request was sent on submit (D-05 — fake submit). Close the modal with ×, click-outside, and Escape — all should close it.
    4. Click the хоротерапия «ближайшая встреча ↗» link in the modal — confirm it opens the real Timepad event in a new tab.
    5. Check the footer «слушать» and «связь» link lists match the draft (Яндекс/Apple/VK/Instagram are still `#` placeholders — expected, Phase 4).
    6. On mobile width: confirm the footer stacks to one column and the modal becomes single-column.
    7. View source one more time: confirm zero `googleapis`/`gstatic` and that the noindex meta is present.
  </how-to-verify>
  <resume-signal>Type "approved" if the full page matches the draft, animations work, the modal opens/submits/closes correctly with no network call, and no Google Fonts appear — or describe any mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → Vercel CDN | Static prerendered markup; no server-side processing |
| Booking form → (nowhere) | Phase 1 form submit is a fake demo — input is NOT transmitted anywhere (D-05) |
| Page → external destinations | Footer/streaming/social/Timepad links open in new tabs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure / Injection | Lessons-modal demo form input (name, contact) | accept | The demo submit handler calls `e.preventDefault()` and makes NO fetch/XHR (D-05) — no input crosses any trust boundary, so no input-validation or injection surface exists in Phase 1. Real validation lands with the Phase 3 Telegram endpoint. Verified by the grep gate (zero `fetch`/`XMLHttpRequest` in demo-modal.js). |
| T-04-02 | Information Disclosure | `target="_blank"` footer/streaming/social/Timepad links | mitigate | All `target="_blank"` anchors carry `rel="noopener noreferrer"` (ASVS L1) |
| T-04-03 | Tampering | `streamingLinks`/`socialLinks` rendered as `href` | accept | Values come from the trusted build-time `site.ts` singleton, not user input; placeholders are `#`/known URLs only |
</threat_model>

<verification>
- `astro check` passes; `NOINDEX=true npm run build` exits 0
- `site.ts` drives footer + tracks-header links (LINK-01/02), Яндекс/VK first
- demo-modal.js makes no network call; submit shows «Спасибо!» (D-05)
- global-animations.js: IntersectionObserver reveal + pointer-fine cursor glow (FND-03)
- Full page top-to-bottom visually faithful to index.html on desktop and mobile (FND-01/04)
- dist/ has zero Google Fonts references; noindex meta present
</verification>

<success_criteria>
- Streaming + social links modeled as data and rendered from site.ts (LINK-01, LINK-02)
- Lessons modal + fake submit isolated for clean Phase 3 swap (D-05)
- Scroll-reveal + cursor-glow animations match the draft (FND-03)
- Whole page pixel-faithful and responsive (FND-01, FND-04)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-static-site/01-04-SUMMARY.md` when done. Record: the lessons.jpg background approach chosen (imported asset URL vs public/), the LessonsModal placement (Layout vs index.astro), the final Vercel preview URL, and a consolidated list of every remaining `#`/placeholder href + the draft email — the complete Phase 4 verification tracker (ticket URLs, Яндекс/VK Музыка, Apple Music, VK community, Instagram, hello@brusnika.ru).
</output>
