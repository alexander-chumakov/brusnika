---
phase: 03.1-day-night-theme
plan: 04
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03"]
files_modified:
  - src/styles/global.css
  - src/components/Nav.astro
  - public/icons/brusnika-toggle-night.svg
  - public/icons/brusnika-toggle-day.svg
autonomous: false
requirements: [D-01, D-04]
must_haves:
  truths:
    - "The user has seen the full day theme working in both Direction A (Clean White) and Direction B (Warm Cream) and chosen one"
    - "The chosen day palette direction is the permanent [data-theme=day] block in global.css"
    - "The user has seen both toggle icon variants (sun/moon and lingonberry) and chosen one"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Final chosen day palette direction in [data-theme=day]"
      contains: "[data-theme=\"day\"]"
  key_links:
    - from: "user visual pick"
      to: "global.css [data-theme=day] values"
      via: "manual selection after side-by-side comparison"
      pattern: "\\[data-theme=\"day\"\\]"
---

<objective>
Resolve the two explicit design-first, user-by-sight decisions that the UI-SPEC and CONTEXT deferred to the user: (D-01) which day palette direction — Clean White vs Warm Cream — and (D-04) which toggle icon — sun/moon vs the брусника lingonberry variant. Plan 01 shipped Warm Cream + sun/moon as the working defaults; this plan presents the alternatives, captures the user's choice, and commits the chosen palette + icon as permanent.

Purpose: D-01 and D-04 are NOT planner/executor decisions — the user explicitly wants to compare and pick by sight. This plan is the "show both, user picks" step. It runs last because comparison only makes sense once the whole theme switches end-to-end (Waves 1-2 complete).

Output: Final day palette direction committed; final toggle icon committed; both prototype artifacts created so the user can actually compare.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-day-night-theme/03.1-CONTEXT.md
@.planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md
@.planning/phases/03.1-day-night-theme/03.1-01-PLAN.md
@.planning/phases/03.1-day-night-theme/03.1-02-PLAN.md
@.planning/phases/03.1-day-night-theme/03.1-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Direction A (Clean White) comparison + author lingonberry icon variant</name>
  <files>src/styles/global.css, public/icons/brusnika-toggle-night.svg, public/icons/brusnika-toggle-day.svg, src/components/Nav.astro</files>
  <read_first>
    - src/styles/global.css (the `[data-theme="day"]` block from Plan 01 — currently Warm Cream / Direction B)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (sections "Day Palette — Two Directions" with the full Direction A vs B hex tables, and "Toggle Control" -> "Variant 2 — Брусника brand icon")
    - .planning/phases/03.1-day-night-theme/03.1-CONTEXT.md (D-01 user wants to compare clean-white vs warm-cream; D-04 user likes a custom брусника icon)
    - src/components/Nav.astro (the toggle button + Variant 1 sun/moon inline SVGs added in Plan 01)
  </read_first>
  <action>
    Prepare BOTH comparison artifacts so the user can evaluate by sight: (a) Produce a temporary way to view Direction A (Clean White) alongside the live Direction B. Simplest non-invasive approach: add a temporary `[data-theme="day-a"]` block in global.css populated with the Direction A "Clean White" column from the UI-SPEC (`--color-bg` #ffffff, `--color-bg-raised`/`--color-surface-card` #f8f8f8, `--color-bg-modal` #ffffff, and all surface/overlay/gradient day-A values; text + accent tokens are identical to Direction B). Document the one-liner to preview it: `document.documentElement.setAttribute('data-theme','day-a')`. This is a PROTOTYPE-ONLY scaffold — it MUST be removed in Task 3 after the pick. (b) Author the Variant 2 lingonberry icon: create `public/icons/brusnika-toggle-night.svg` (lingonberry sprig with a subtle night treatment) and `public/icons/brusnika-toggle-day.svg` (same sprig, lighter/sun-washed fill, no glow), single-path SVGs, 20x20 viewBox, currentColor-friendly so `var(--color-text-muted)`/`var(--color-text)` apply. Wire a temporary commented-out alternate icon block in Nav.astro (or a `data-icon-variant` switch) so the user can see Variant 2 next to the live Variant 1 sun/moon. Implements the prototype half of D-01 + D-04.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "day-a proto block:"; grep -c '\[data-theme="day-a"\]' src/styles/global.css; echo "lingonberry svgs:"; ls public/icons/brusnika-toggle-night.svg public/icons/brusnika-toggle-day.svg 2>&1 | grep -c "brusnika-toggle"</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme="day-a"]` prototype block exists in global.css with the Clean White values (or an equivalent documented compare mechanism)
    - `public/icons/brusnika-toggle-night.svg` and `public/icons/brusnika-toggle-day.svg` both exist and are valid 20x20 SVGs
    - Both toggle icon variants are viewable (Variant 1 live; Variant 2 available via the temporary switch)
    - `npm run build` exits 0
    - The prototype scaffolding is clearly marked as temporary (comment) for removal in Task 3
  </acceptance_criteria>
  <done>Direction A comparison block + lingonberry SVG icons created and viewable alongside the live Direction B + sun/moon; build passes; scaffold marked temporary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User picks day palette direction (D-01) and toggle icon (D-04)</name>
  <files>(none — user-decision checkpoint, no files changed)</files>
  <action>Pause for the user. Do NOT edit any files in this task. Present the working theme and the two comparison axes (palette direction + toggle icon) per how-to-verify below, then wait for the user's two picks before proceeding to Task 3. This checkpoint exists because D-01 and D-04 are explicitly user-by-sight decisions (CONTEXT.md) — the executor must not choose on the user's behalf.</action>
  <what-built>
    The full day/night theme is live and switches end-to-end. Two day palette directions are available to compare (Direction B / Warm Cream is the live default; Direction A / Clean White via the temporary `data-theme="day-a"`), and two toggle icons are available (sun/moon live; брусника lingonberry via the temporary switch).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the site. Click the top-right toggle to enter day mode — this shows Direction B (Warm Cream).
    2. To preview Direction A (Clean White), in the browser devtools console run: `document.documentElement.setAttribute('data-theme','day-a')`. Compare the two day looks across hero, shows, gallery, the now-playing player bar, and an open lessons/video modal.
    3. Decide your day palette: Direction A (Clean White, #ffffff bg) or Direction B (Warm Cream, #f5f0ea bg).
    4. Compare toggle icons: the live sun/moon vs the брусника lingonberry SVGs in `public/icons/`. Decide which icon you want.
    5. Reply with BOTH choices, e.g. "palette: warm cream, icon: lingonberry".
  </how-to-verify>
  <verify><human-check>User has replied with both picks: a day palette direction (A/B) and a toggle icon (sun-moon/lingonberry).</human-check></verify>
  <resume-signal>Reply with your two picks: day palette direction (A clean-white / B warm-cream) AND toggle icon (sun-moon / lingonberry).</resume-signal>
  <done>User has provided both decisions; executor records them for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the chosen palette + icon; remove prototype scaffolding</name>
  <files>src/styles/global.css, src/components/Nav.astro, public/icons/brusnika-toggle-night.svg, public/icons/brusnika-toggle-day.svg</files>
  <read_first>
    - The user's two picks from Task 2 (palette direction + icon variant)
    - src/styles/global.css (the `[data-theme="day"]` block + the temporary `[data-theme="day-a"]` prototype block)
    - src/components/Nav.astro (Variant 1 + temporary Variant 2 switch)
    - .planning/phases/03.1-day-night-theme/03.1-UI-SPEC.md (the chosen direction's exact hex column)
  </read_first>
  <action>
    Apply the user's decisions and clean up: (a) PALETTE — if the user chose Direction A (Clean White), overwrite the permanent `[data-theme="day"]` token values with the Direction A column from the UI-SPEC (bg/surface/overlay/gradient values; text + accent are unchanged across directions). If the user chose Direction B (Warm Cream), keep the existing values. Then DELETE the temporary `[data-theme="day-a"]` prototype block entirely. (b) ICON — if the user chose the брусника lingonberry, make `public/icons/brusnika-toggle-*.svg` the active toggle icons in Nav.astro (inline `<img>` or inline SVG per UI-SPEC) and remove the sun/moon inline SVGs; if the user chose sun/moon, keep Variant 1 live and DELETE the unused lingonberry SVG files (unless the user asks to retain — confirm). Remove the temporary `data-icon-variant` switch. (c) Verify night theme still pixel-identical and `[data-theme="day"]` reflects exactly one chosen direction. Implements the commit half of D-01 + D-04.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "day block (expect 1):"; grep -c '\[data-theme="day"\]' src/styles/global.css; echo "no proto day-a (expect 0):"; grep -c '\[data-theme="day-a"\]' src/styles/global.css; echo "no temp icon switch (expect 0):"; grep -c "data-icon-variant" src/components/Nav.astro</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme="day"]` block contains exactly the chosen direction's values (A or B)
    - `grep -c '\[data-theme="day-a"\]' src/styles/global.css` returns 0 (prototype removed)
    - The active toggle icon matches the user's choice; the temporary `data-icon-variant` switch is removed
    - If sun/moon chosen, lingonberry SVGs removed (unless user asked to keep); if lingonberry chosen, sun/moon inline SVGs removed
    - Night theme pixel-identical; `npm run build` exits 0
  </acceptance_criteria>
  <done>Chosen day palette direction is the sole permanent `[data-theme="day"]` block; chosen toggle icon is live; all prototype scaffolding removed; build passes; night unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static SVG assets | Two hand-authored SVG files served from `public/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06 | Tampering / XSS | `public/icons/brusnika-toggle-*.svg` | mitigate | SVGs are hand-authored static assets with no embedded `<script>`, no external `href`, no event handlers — pure `<path>`/`<svg>` markup. Author them inert; if inlined into Nav, ensure no untrusted interpolation. |
| T-03.1-07 | (config) | `[data-theme]` palette swap | accept (no surface) | Palette values are static CSS literals chosen by the developer/user; no runtime input. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept (none) | No dependencies added (UI-SPEC Registry Safety). No install tasks; legitimacy gate not applicable. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Exactly one `[data-theme="day"]` block remains (chosen direction); no `[data-theme="day-a"]` prototype.
- Active toggle icon matches the user's pick; no temporary switch remains.
- Night theme pixel-identical to pre-phase.
</verification>

<success_criteria>
- D-01 resolved by the user by sight; chosen day palette is permanent.
- D-04 resolved by the user by sight; chosen toggle icon is live.
- All prototype/comparison scaffolding removed; clean final state; build passes.
</success_criteria>

<output>
Create `.planning/phases/03.1-day-night-theme/03.1-04-SUMMARY.md` when done. Record the user's two picks (palette direction + icon) and confirm scaffolding removal.
</output>
