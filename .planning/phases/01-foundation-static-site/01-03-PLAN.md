---
phase: 01-foundation-static-site
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/content.config.ts
  - src/content/tracks/
  - src/content/shows/
  - src/content/gallery/
  - src/components/TracksSection.astro
  - src/components/ShowsSection.astro
  - src/components/GallerySection.astro
  - src/scripts/demo-player.js
  - src/pages/index.astro
autonomous: false
requirements: [FND-01, FND-04, FND-05, SHOW-01, SHOW-02]
must_haves:
  truths:
    - "Visitor sees the «Синглы и любимое» track list (5 tracks with number, title, subtitle, duration) rendered from a Content Collection"
    - "Clicking a track «слушать» pops the fake sticky now-playing bar with that track's title and number (draft demo behavior)"
    - "Visitor sees the «Услышать вживую» shows section listing 4 upcoming shows (date, year, city, venue) each with a «билеты» link, rendered from a Content Collection"
    - "Visitor sees the «Между песнями» photo gallery (5 photos) in the draft's grid, rendered from a Content Collection, images lazy-loaded"
    - "Tracks/shows/gallery sections all read via getCollection() — no hardcoded section data in components (the v1→v2 CMS seam, D-07)"
  artifacts:
    - path: "src/content.config.ts"
      provides: "tracks, shows, gallery collection definitions with zod schemas (D-09 optional media fields on tracks)"
      contains: "defineCollection"
    - path: "src/components/TracksSection.astro"
      provides: "track list from getCollection('tracks') + demo player markup"
      min_lines: 25
    - path: "src/components/ShowsSection.astro"
      provides: "shows list from getCollection('shows') with ticket links (SHOW-01/02); sample/draft show entries per D-01"
    - path: "src/components/GallerySection.astro"
      provides: "photo grid from getCollection('gallery'), lazy <Image>"
    - path: "src/scripts/demo-player.js"
      provides: "isolated throwaway now-playing-bar behavior (D-04)"
  key_links:
    - from: "src/components/TracksSection.astro"
      to: "getCollection('tracks')"
      via: "Astro content query in frontmatter"
      pattern: "getCollection\\(['\"]tracks"
    - from: "src/components/ShowsSection.astro"
      to: "getCollection('shows')"
      via: "Astro content query in frontmatter"
      pattern: "getCollection\\(['\"]shows"
    - from: "src/components/GallerySection.astro"
      to: "getCollection('gallery')"
      via: "Astro content query in frontmatter"
      pattern: "getCollection\\(['\"]gallery"
---

<objective>
Introduce the Content Layer — the v1→v2 CMS seam (FND-05) — and render the three list-driven sections from it: Tracks, Shows, and Gallery. This is the first real data read in the project. Define `src/content.config.ts` with zod schemas (tracks carry forward-compatible optional media fields per D-09), author the JSON content from the draft's existing entries, and build the three sections via `getCollection()`. Port the draft's throwaway now-playing-bar demo (D-04) as an isolated script.

Purpose: Prove and establish the content-as-data pattern that every list section uses, so a v2 CMS is a loader swap with zero component changes. Shows section also delivers SHOW-01/02.
Output: A Vercel preview where the track list, shows list (with «билеты» links), and photo grid all render from Content Collections, and clicking a track shows the fake now-playing bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-static-site/01-CONTEXT.md
@.planning/phases/01-foundation-static-site/01-RESEARCH.md
@.planning/phases/01-foundation-static-site/01-02-SUMMARY.md

<!-- Pixel source of truth. TRACKS = index.html lines 131-174; SHOWS = 191-226; GALLERY = 228-241; NOW-PLAYING BAR markup = 312-326; demo player JS = 413-425. -->
@index.html

<interfaces>
<!-- Schema contract the JSON files and components implement. From RESEARCH § "content.config.ts — Complete for Phase 1" + D-09. -->
tracks schema (src/content/tracks/*.json): number(string), title(string), subtitle?(string),
  displayDuration?(string), audioUrl?(string) [Phase 2 fill — empty now], durationSeconds?(number) [Phase 2 fill — empty now].
shows schema (src/content/shows/*.json): dateDisplay(string), year(string), city(string), venue(string),
  ticketUrl?(string).default('#')  [D-03 placeholder '#' now; Phase 4 fills real Timepad/Kassir URLs].
gallery schema (src/content/gallery/*.json): filename(string), alt(string),
  gridSpan(enum 'single'|'tall').default('single'), order(number).
Import z from 'astro/zod' (NOT the 'zod' package — RESEARCH Pitfall 5). Use glob() loader from 'astro/loaders'.
Insertion order in index.astro (draft order): Hero, Marquee, FeaturedAlbum, [Tracks], About, [Shows], [Gallery], Sing.
  Tracks goes immediately after FeaturedAlbum; Shows + Gallery go between About and Sing.
Demo player isolation (D-04): scripts/demo-player.js is a self-contained IIFE reading .track[data-i][data-title]
  and toggling #now-playing / #np-title / #np-index. Phase 2 deletes this file + its <script> tag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: content.config.ts + JSON content for tracks, shows, gallery</name>
  <files>src/content.config.ts, src/content/tracks/, src/content/shows/, src/content/gallery/</files>
  <read_first>
    - index.html lines 142-173 (the 5 track buttons — exact number/title/subtitle/duration values; e.g. 01 Доодури · сингл 2026 · 4:18, 05 Вспомни меня · feat. YERKATT · 2:01)
    - index.html lines 205-224 (the 4 show rows — date/year/city/venue: 12 сент 2026 Москва «16 тонн»; 27 сент 2026 Санкт-Петербург «Сердце»; 11 окт 2026 Казань Смена; 25 окт 2026 Екатеринбург Дом Печати)
    - index.html lines 235-239 (the 5 gallery images and their src order: g-studio.jpg [tall, grid-row span 2], g-guitar.jpg, band-guitars.jpg, g-couch.jpg, live.jpg)
    - .planning/phases/01-foundation-static-site/01-RESEARCH.md § "content.config.ts — Complete for Phase 1", § "Sample JSON Content Files", § "Pitfall 5" (astro/zod)
  </read_first>
  <action>
    Create `src/content.config.ts` defining three collections with `defineCollection` + `glob({ pattern: '**/*.json', base: './src/content/<name>' })` loaders and the zod schemas in this plan's `<interfaces>` block. Import `z` from `astro/zod` and `glob` from `astro/loaders`. The tracks schema MUST include the optional `audioUrl` and `durationSeconds` fields (D-09) so Phase 2 needs no migration. The shows `ticketUrl` defaults to `'#'` (D-03). Export `collections = { tracks, shows, gallery }`.

    Author the JSON content files from the draft's existing values:
    - `src/content/tracks/01-doorudi.json` … `05-vspomni-menya.json`: number "01".."05", title, subtitle (the «· …» suffix), displayDuration ("4:18","3:26","3:11","2:58","2:01"). Leave audioUrl/durationSeconds absent.
    - `src/content/shows/01-moskva.json` … `04-ekaterinburg.json`: dateDisplay, year "2026", city, venue. Leave ticketUrl absent (schema default '#').
    - `src/content/gallery/01-studio.json` … `05-live.json`: filename (g-studio.jpg / g-guitar.jpg / band-guitars.jpg / g-couch.jpg / live.jpg), alt "внимание брусника!", gridSpan ("tall" for g-studio only, "single" otherwise), order 1..5.
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npm run build 2>&1 | tail -5 && ls src/content/tracks/*.json | wc -l | grep -q 5 && ls src/content/shows/*.json | wc -l | grep -q 4 && ls src/content/gallery/*.json | wc -l | grep -q 5 && grep -q "astro/zod" src/content.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (zod schema validates all JSON — build fails loudly on any schema violation)
    - 5 track JSON, 4 show JSON, 5 gallery JSON files exist
    - `src/content.config.ts` imports `z` from `astro/zod` (NOT `zod`) and defines tracks/shows/gallery
    - tracks schema includes optional `audioUrl` and `durationSeconds` (D-09); shows `ticketUrl` defaults to `'#'` (D-03)
  </acceptance_criteria>
  <done>Content Layer config + all JSON content authored from the draft; build validates the schemas.</done>
</task>

<task type="auto">
  <name>Task 2: TracksSection (+ demo player), ShowsSection, GallerySection, compose into index.astro</name>
  <files>src/components/TracksSection.astro, src/components/ShowsSection.astro, src/components/GallerySection.astro, src/scripts/demo-player.js, src/pages/index.astro</files>
  <read_first>
    - index.html lines 131-174 (TRACKS section: header «Синглы и любимое» + Spotify/Яндекс/YouTube links, the 5 .track buttons with data-i/data-title, grid-template-columns:60px 1fr auto auto, «▶ слушать», .track-time)
    - index.html lines 191-226 (SHOWS section id="shows": eyebrow «концерты», h2 «Услышать вживую», live-crowd2.jpg banner with caption, the .show-row grid 140px 1fr auto, «билеты» .ticket-link)
    - index.html lines 228-241 (GALLERY section: eyebrow «кадры», h2 «Между песнями», .gallery-grid 1.4fr 1fr 1fr / 210px 210px, first item grid-row:span 2)
    - index.html lines 312-326 (NOW-PLAYING BAR markup #now-playing/#np-title/#np-index/#np-close) and lines 413-425 (the demo player IIFE)
    - .planning/phases/01-foundation-static-site/01-RESEARCH.md § "Pattern 1" (getCollection usage), § "Pattern 7" (demo JS isolation, is:inline)
    - src/pages/index.astro and src/content.config.ts from Task 1
  </read_first>
  <action>
    Create `src/components/TracksSection.astro`: in frontmatter `const tracks = (await getCollection('tracks')).sort((a,b) => a.data.number.localeCompare(b.data.number))`. Port the header (h3 «Синглы и любимое» + the three streaming anchors — keep the draft's current hrefs for now: Spotify real, «Яндекс Музыка» `#` placeholder per D-03, YouTube real, all `target="_blank" rel="noopener noreferrer"`). Map tracks into `.track` buttons: each `<button class="track" data-i={t.data.number} data-title={t.data.title}>` with the Prata number, Prata title + Golos subtitle span, «▶ слушать» pink label, and the `.track-time` displayDuration. Match the draft grid + borders exactly (first row border-top, last row border-bottom). Also render the now-playing-bar markup (`#now-playing` with EQ bars, `#np-title`, `#np-index`, `#np-close`) from lines 312-326 — fixed bottom, `display:none` initially. Scoped styles incl. the mobile `@media` rule (40px 1fr auto; hide `.track-time`).
    Create `src/scripts/demo-player.js`: verbatim port of the now-playing IIFE (lines 413-425). Self-contained, isolated. Reference it from TracksSection (or index.astro) with `<script src="../scripts/demo-player.js" is:inline></script>` — confirm `is:inline` vs bare `<script>` behavior against current Astro docs (RESEARCH assumption A2); pick whichever preserves the verbatim IIFE without bundling errors and record the choice in the summary.

    Create `src/components/ShowsSection.astro`: `const shows = await getCollection('shows')` (preserve file order via filename or an order field if needed). Port `<section id="shows">`: eyebrow «концерты», h2 «Услышать <span accent>вживую</span>», the live-crowd2.jpg banner (`<Image>`, `format="webp"`, `loading="lazy"`, 21/9, object-position center 30%) with its Prata caption, then map shows into `.show-row` grids: left Prata `dateDisplay` + muted `year`, middle city + venue, right `<a class="ticket-link" href={s.data.ticketUrl} ...>билеты</a>` (SHOW-01 lists date/city/venue; SHOW-02 ticket link — `#` placeholder now per D-03, real in Phase 4). First row border-top, last border-bottom. Scoped styles incl. mobile rule (96px 1fr auto).

    Create `src/components/GallerySection.astro`: `const photos = (await getCollection('gallery')).sort((a,b)=>a.data.order-b.data.order)`. Port the gallery: eyebrow «кадры», h2 «Между <span accent>песнями</span>», `.gallery-grid` (1.4fr 1fr 1fr / 210px 210px). For each photo render a `.gallery-item` wrapper (the first / `gridSpan:'tall'` item gets `grid-row:span 2`), with an `<Image>`. Because filenames are dynamic strings, use Vite glob import: `const galleryImages = import.meta.glob('../assets/images/*.jpg')` (or import.meta.glob with `{ eager: true, import: 'default' }`) and look up by `photo.data.filename` — all gallery images lazy-loaded (`loading="lazy"`, `widths={[400,800]}`, `sizes="(max-width:860px) 50vw, 33vw"`, `format="webp"`). Keep the repeating-linear-gradient placeholder bg and `.gallery-item:hover` scale. Scoped styles incl. the mobile rule (1fr 1fr; first item grid-row auto). NOTE: this is a static gallery only — fullscreen lightbox is Phase 2 (GAL-01..03), do NOT wire PhotoSwipe here.

    Update `src/pages/index.astro`: import TracksSection, ShowsSection, GallerySection and insert them at the Plan-02 placement comments in draft order — Tracks immediately after FeaturedAlbum; Shows and Gallery between About and Sing. Add the demo-player `<script>` reference if not already in TracksSection.
  </action>
  <verify>
    <automated>cd /Users/chumakov/projects/brusnika && npm run build 2>&1 | tail -3 && grep -q "getCollection('tracks')\|getCollection(\"tracks\")" src/components/TracksSection.astro && grep -q "getCollection('shows')\|getCollection(\"shows\")" src/components/ShowsSection.astro && grep -q "getCollection('gallery')\|getCollection(\"gallery\")" src/components/GallerySection.astro && grep -q 'now-playing' src/components/TracksSection.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - TracksSection, ShowsSection, GallerySection each call `getCollection(...)` and contain NO hardcoded list data (FND-05 — the v1→v2 seam)
    - TracksSection renders 5 `.track` buttons each carrying `data-i` and `data-title`, and includes the `#now-playing` bar markup
    - ShowsSection renders 4 `.show-row`s each with a `.ticket-link` whose `href` is the schema `ticketUrl` (SHOW-01/02)
    - GallerySection renders 5 `.gallery-item`s with lazy optimized `<Image>` (webp derivatives in `dist/`); first item spans 2 rows
    - `index.astro` composes all sections in the draft's document order
    - `src/scripts/demo-player.js` is a verbatim, isolated IIFE (D-04)
  </acceptance_criteria>
  <done>Tracks, shows, and gallery sections render from Content Collections; demo now-playing bar wired; all composed in draft order.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify tracks/shows/gallery + demo player on Vercel preview</name>
  <what-built>The three data-driven sections (tracks, shows, gallery) rendered from Astro Content Collections, plus the draft's throwaway now-playing-bar demo, deployed to a Vercel preview.</what-built>
  <action>Human verification only — no code changes. Deploy the latest build to a Vercel preview, then walk the steps in <how-to-verify> below and compare against index.html. Resolve any mismatch before approving.</action>
  <how-to-verify>
    1. Open the preview URL. After the featured album, confirm the «Синглы и любимое» list shows all 5 tracks (number, title, «· …» subtitle, duration) exactly as in `index.html`.
    2. Click any track's row. Confirm the fake sticky now-playing bar slides up from the bottom showing «сейчас играет · демо», the track title, and «трек NN». Click × to dismiss it. (This is the throwaway demo — real audio is Phase 2.)
    3. Scroll to «Услышать вживую». Confirm the live banner + caption, then the 4 shows (12 сент Москва «16 тонн»; 27 сент Санкт-Петербург «Сердце»; 11 окт Казань Смена; 25 окт Екатеринбург Дом Печати), each with a «билеты» button. The button still points to `#` — that is expected (real ticket URLs are Phase 4).
    4. Scroll to «Между песнями». Confirm the 5-photo grid matches the draft layout (the studio photo spans two rows on the left); photos load as you scroll (lazy).
    5. On mobile width: confirm the track time column hides, the show rows reflow, and the gallery becomes a 2-column grid.
  </how-to-verify>
  <resume-signal>Type "approved" if tracks/shows/gallery render correctly from data, the demo now-playing bar works, and the layout matches the draft on desktop and mobile — or describe any mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → Vercel CDN | Static prerendered markup; content read at build time from local JSON |
| Build env → content JSON | Local trusted JSON files validated by zod at build time |
| Page → external destinations | Streaming links + ticket links (placeholder `#` now) open in new tabs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Build-time content from `src/content/*.json` | mitigate | zod schemas in `content.config.ts` validate every entry at build; malformed/unexpected data fails the build rather than rendering silently |
| T-03-02 | Information Disclosure | `target="_blank"` streaming + ticket links | mitigate | All `target="_blank"` anchors carry `rel="noopener noreferrer"` |
| T-03-03 | Tampering | Demo player IIFE reads `data-title`/`data-i` from the DOM | accept | Values originate from build-time trusted JSON, not user input; throwaway demo replaced in Phase 2; only writes to `textContent` (no innerHTML injection) |
</threat_model>

<verification>
- `npm run build` exits 0; zod validates all content JSON; `astro check` passes
- All three sections read via getCollection() with no hardcoded list data
- Demo now-playing bar shows/dismisses on track click
- Ticket links render from schema `ticketUrl` (placeholder `#` tracked for Phase 4)
- Mobile @media rules reflow tracks/shows/gallery
- Vercel preview faithful to the draft's tracks/shows/gallery sections
</verification>

<success_criteria>
- Content Collections established as the v1→v2 CMS seam (FND-05)
- Tracks, shows, gallery sections render from data, pixel-faithful (FND-01)
- Shows list date/city/venue + ticket links (SHOW-01, SHOW-02)
- Responsive reflow matches the draft (FND-04)
- Demo now-playing bar isolated for clean Phase 2 swap (D-04)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-static-site/01-03-SUMMARY.md` when done. Record: the resolved `is:inline` vs bare `<script>` decision (RESEARCH assumption A2), the gallery dynamic-image-import approach used, every `#`/placeholder href still present (for the Phase 4 tracker), and the getCollection sort approach for each collection.
</output>
